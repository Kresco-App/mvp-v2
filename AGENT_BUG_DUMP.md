# 🐞 Agent Bug & Misimplementation Dump

**Instructions for AI Agents:**
1. **Deduplication Check:** Before adding a new finding, always review the list below to ensure it hasn't already been reported.
2. **Logging Format:** Add any suspicious implementations, vulnerabilities, or bugs as an uncompleted checklist item `- [ ]` at the bottom of the list.
3. **Master Agent:** The master agent can review and check off items `- [x]` when they are resolved, or mark them as `[FALSE POSITIVE]` or `[ASSIGNED]` inline.

## 📋 Suspicious Findings Checklist

- [x] **[HIGH]** `backend/example.py` - Found hardcoded AWS keys in `get_s3_client()`. Should use environment variables.
- [ ] **[CRITICAL]** `backend/.env` - Found live production secrets in plain text. Need to use placeholder values for `.env.example` or dummy values locally.
- [ ] **[HIGH]** `backend/app/routers/courses.py` - Insecure Gamification endpoint (`/topic-items/{item_id}/complete`) trusts `body.score` and `body.watched_seconds` submitted by the client, allowing leaderboard spoofing.
- [ ] **[HIGH]** `backend/app/routers/gamification.py` - `/update` endpoint trusts `body.watched_seconds` without server-side validation, allowing auto-completion of lessons.
- [ ] **[MEDIUM]** `backend/app/routers/users.py` - Weak password requirements in `/auth/signup` allows passwords under 6 characters. Needs to be at least 8.
- [ ] **[HIGH]** `backend/app/routers/users.py` - Account hijacking / DOS in `/auth/signup`. Allows overwriting `full_name` and `password` for existing unverified accounts, preventing legitimate user from verifying.
- [ ] **[HIGH]** `backend/app/routers/gamification.py` - ID / Model Mismatch in `/complete`. Hardcoded to query `ChapterSection` ignoring `Quiz` or `ChapterBlock`, returning 404s for quizzes or incorrect authorization.
- [ ] **[MEDIUM]** `backend/app/routers/interactions.py` - Data Over-fetching in `/comments`. Uses `selectinload(Comment.replies)` and calculates length in Python, loading entire relation into memory instead of a SQL count.
- [ ] **[LOW]** `backend/app/routers/users.py` - Email Spam Abuse. Unauthenticated endpoints `/auth/resend-verification` and `/auth/forgot-password` only rely on IP rate limits, which can be bypassed.
- [ ] **[HIGH]** `backend/app/admin/auth.py` and `backend/app/models/users.py` - Weak default password assignment using the magic string `!` and hardcoded checks against it (`if user.password == "!"`).
- [x] **[HIGH]** `backend/app/models/users.py` (and others) - Contains multiple `except Exception:` blocks that silently swallow all exceptions without logging or re-raising, making debugging difficult.
- [ ] **[MEDIUM]** `backend/find_n1.py` - Presence of N+1 query finder script indicates existing or recurring N+1 database query issues that need a comprehensive review across the codebase.
- [ ] **[LOW]** `backend/.env.example` - `JWT_SECRET_KEY=change-me` does not meet the 32-character minimum requirement enforced by `test_startup_security.py`, which will cause startup failures for new developers.
- [ ] **[LOW]** `backend/alembic.ini` - Contains the hardcoded default database connection string `driver://user:pass@localhost/dbname`.
- [ ] **[HIGH]** `backend/app/models/*.py` - Missing database indexes (`index=True`) on foreign keys. SQLAlchemy does not index foreign keys by default in PostgreSQL, causing severe scalability issues and full table scans on joins/CASCADE deletes.
- [ ] **[HIGH]** `backend/app/routers/courses.py` - Missing API pagination on list endpoints (`list_topics`, `list_subjects`). Loading the entire dataset into memory will lead to memory exhaustion as the course content grows.
- [ ] **[MEDIUM]** `backend/app/models/users.py` - High-frequency query columns like `stripe_customer_id` and `is_active` lack indexes, which could cause slow sequential scans on webhooks and authentication operations.
- [ ] **[MEDIUM]** `frontend` - Found raw HTML `<img>` tags in multiple files (`figma-audit/page.tsx`, `zed/Scratchpad.tsx`, `lib/topicWorkspaceViewModel.ts`) bypassing Next.js `<Image>` optimization, which can degrade frontend performance.
- [ ] **[HIGH]** `backend/app/routers/*.py` - Missing API pagination across almost all routers (`calendar`, `gamification`, `interactions`, `notifications`, `professor`) using unpaginated `.scalars().all()` calls. This will cause memory exhaustion as data grows.
- [ ] **[HIGH]** `frontend/app/(dashboard)/exam/[subjectId]/page.tsx` - Timer Reset Bug in Exam Mode. The `setInterval` for the countdown timer is inside a `useEffect` that depends on `handleSubmit`, which changes on every answer selection, causing the timer to artificially pause and delay.
- [ ] **[MEDIUM]** `frontend/app/page.tsx` - Missing Post-Login Redirect State. Successful login always redirects to `/home`, losing user context from deep links (missing `?next=` handling).
- [ ] **[LOW]** `frontend/components/Leaderboard.tsx` - Suboptimal `next/image` usage with the `unoptimized` flag, hurting client performance by loading full-resolution avatars.
- [ ] **[LOW]** `backend/find_n1.py` - Silently ignores file-parsing errors using `except Exception as e: pass`, completely bypassing any files that might have parsing issues.
- [ ] **[CRITICAL]** `backend/app/routers/gamification.py` - The `get_leaderboard` endpoint calculates `rank()` over the filtered result set. If a user provides a `search` parameter, their rank will be calculated relative only to other users matching the search (meaning they could easily show up as rank 1). Furthermore, calculating `rank()` over the entire `UserXP` table dynamically on every request without caching will cause a full table scan and major scalability issues as the user base grows.
- [ ] **[CRITICAL]** `backend/app/routers/professor.py` - In `_conversation_last_sender_role`, the query selects **ALL** messages for the given `conversation_ids` and loads them into memory, just to pick the most recent one using a python dictionary (`if conversation_id not in roles`). For conversations with thousands of messages, this will cause massive memory spikes and database load. It should use `DISTINCT ON (conversation_id)` or a window function subquery.
- [ ] **[HIGH]** `backend/app/routers/courses.py` - In `_grade_quiz_question` for `image_hotspot` questions, `safe_rx` and `safe_ry` are calculated using `region.get("rx") - cursor.get("radius")`. A malicious student can submit a negative `radius` (e.g., `-10000`), which inflates `safe_rx` and `safe_ry` to massive positive numbers. This makes the elliptical boundary check `(dx^2 / rx^2) + (dy^2 / ry^2) <= 1` trivially evaluate to `True`, allowing them to bypass and cheat on hotspot questions automatically.
- [ ] **[HIGH]** `backend/app/routers/payments.py` - In `stripe_webhook`, when handling `charge.dispute.created`, if the customer ID cannot be resolved, the server throws an `HTTPException(status_code=502)`. This causes Stripe to not receive a 200 OK, meaning Stripe will continuously retry the webhook delivery, potentially leading to webhook blocking or rate limiting issues. It should log the error and return 200 OK.
- [ ] **[MEDIUM]** `backend/app/routers/users.py` - In `verify_email`, if the user has a `professor` role, `require_professor_active_offering` is called before setting `is_email_verified = True`. However, `require_professor_active_offering` inherently throws an error if the professor does not have an active offering. A newly registered professor without an offering will be permanently unable to verify their email, leading to a catch-22 edge case.
- [ ] **[MEDIUM]** `backend/app/routers/gamification.py` - In `get_subject_plan`, `LessonProgress.lesson_id.in_(all_lesson_ids)` is executed without checking if `all_lesson_ids` is empty. If a subject has no lessons, this triggers a query with an empty `IN` clause (`IN (NULL)`), which throws an SAWarning and degrades database performance unnecessarily. It should be wrapped in `if all_lesson_ids:`.
- [ ] **[LOW]** `backend/app/routers/courses.py` - In `submit_tab_quiz`, there is an unnecessary explicit row-level lock on the `User` table (`select(User.id)...with_for_update()`). Since quiz attempts use an idempotency hash and catch `IntegrityError` to handle race conditions, locking the entire `User` row is an anti-pattern that creates a bottleneck and potential deadlocks across all user mutations.
- [ ] **[LOW]** `backend/app/routers/professor.py` - In `_require_student_live_session`, the validation checks if `ProgramTrack.niveau == student.niveau` and `filiere == student.filiere`, but it fails to check if the `CourseOffering` is actually `status == "active"`. A student could potentially join live sessions for archived or inactive course offerings.
- [ ] **[CRITICAL]** `frontend/proxy.ts` - Missing Next.js Middleware. Named `proxy.ts` instead of `middleware.ts`, so Next.js ignores it, bypassing CSP, secure headers, and SSR authentication bounds.
- [ ] **[HIGH]** `frontend/components/animated/source-ports/**/Latex.tsx` - XSS in interactive Latex Renderers. Error catch block injects formula prop directly into DOM using `innerHTML` on KaTeX failure, allowing arbitrary script execution.
- [ ] **[MEDIUM]** `frontend/lib/authRedirect.ts` - Weak Server-Side Role Validation. `getAuthRedirect` only verifies the presence of a JWT token and does not check user roles for sensitive `/admin` and `/professor` prefixes.
- [ ] **[MEDIUM/LOW]** `frontend/lib/axios.js` - Potential Login CSRF. `isCsrfExemptRequest` explicitly exempts login/signup endpoints from CSRF validation.
- [ ] **[LOW]** `frontend/lib/authSession.ts` - Storing CSRF Tokens in Session Storage. CSRF tokens are written to `sessionStorage` making them trivially accessible during XSS attacks.
- [ ] **[LOW]** `frontend/components/VideoPlayer.jsx` - Missing URI Encoding for External URL Parameters. `otp` and `playback_info` are interpolated into the VdoCipher iframe URL without `encodeURIComponent()`.- [ ] **[HIGH]** `frontend` - Global Zustand Store Destructuring: Across the app (`TopNav.tsx`, `Sidebar.jsx`, etc.), `useAuthStore` is destructured directly (`const { user } = useAuthStore()`), causing massive excessive re-renders whenever any unrelated state in the store changes.
- [ ] **[MEDIUM]** `frontend/app/watch/[lessonId]/page.tsx` & `app/(dashboard)/topics/[topicId]/page.tsx` - Missing App Router Boundaries: Critical routes rely entirely on manual client-side `useEffect` and `useState` fetching instead of leveraging native Next.js `loading.tsx` and `error.tsx` or React Suspense, leading to poor UX on API failures.
- [ ] **[LOW]** `frontend/app/watch/[lessonId]/page.tsx` - Fragile DOM injection: Uses `dangerouslySetInnerHTML` securely wrapped with DOMPurify, but remains a long-term XSS risk if the sanitizer config is ever weakened.
- [ ] **[HIGH]** `backend/app/models/*.py` - Over 15 SQLAlchemy models are missing `index=True` on `ForeignKey` columns (`subject_id`, `chapter_id`, `lesson_id`, `topic_id`, `user_id` etc.), causing severe PostgreSQL locking and full table scans on deletes.
- [ ] **[HIGH]** `backend/app/routers/professor.py` - In `_notify_students_for_live`, it fetches ALL active students into memory and iterates sequentially to `db.add(Notification)` thousands of times instead of using a bulk insert, risking OOM errors and database timeouts.
- [ ] **[MEDIUM]** `backend/app/models/*.py` - High-frequency filter columns used in `WHERE` clauses (e.g., `status` on `Topic`, `Resource`, `Exam`, and `target_id` on `SavedItem`) are missing indexes, causing sequential scans.
- [ ] **[CRITICAL]** `backend/app/models/gamification.py` & `interactions.py` - Missing `UniqueConstraint` on `ContentProgress`, `LessonProgress`, and `SavedItem`. Concurrent requests will insert duplicate rows. Subsequent requests will permanently crash with `MultipleResultsFound` (HTTP 500) because the routers use `scalar_one_or_none()` without `.limit(1)`.
- [ ] **[HIGH]** `backend/app/services/xp.py` - `generate_daily_quests` lacks concurrency protection and `DailyQuest` lacks a unique constraint on `(user_id, date, quest_type)`. Concurrent requests generate duplicate quests for the same day, allowing users to earn double or triple XP by completing the duplicated quests.- [ ] **[HIGH]** `backend/app/schemas/*.py` - Missing `max_length` constraints on almost all Pydantic string fields (e.g., descriptions, content, titles, passwords). This makes the API highly vulnerable to payload-based Denial of Service (DoS) and Out of Memory (OOM) attacks from massive string inputs.
- [ ] **[HIGH]** `backend/app/routers/users.py` - Pre-Account Takeover (Pre-ATO) vulnerability in `/google-login`. If an attacker signs up with a victim's email and sets a password without verifying, and the victim later logs in via Google, the Google login verifies the email but fails to invalidate the squatter's password (e.g., by setting it to `!`). The squatter can then log in using the password and view the victim's data.
- [ ] **[MEDIUM]** `backend/app/schemas/*.py` - Missing `EmailStr` validation for email fields. Relies on basic `str` which can accept invalid email formats into the database.
- [ ] **[CRITICAL]** `.github/workflows/deploy-backend.yml` & `backend/zappa_settings.json` - Hardcoded Secrets Injection: The deployment pipeline uses `render_zappa_settings.py` to bake live production secrets (`DATABASE_URL`, `JWT_SECRET_KEY`, Stripe keys) directly into the Zappa Lambda configuration before deployment. This exposes secrets in plaintext in the AWS Lambda console, CloudTrail logs, and deployment artifacts instead of fetching them securely at runtime via AWS Secrets Manager.
- [ ] **[HIGH]** `backend/app/routers/gamification.py` - Missing Row Locks: Concurrent state mutations for XP points and lesson progress lack database-level locking (`with_for_update()`), leading to race conditions and lost user progress under concurrent requests.
- [ ] **[HIGH]** `backend/app/routers/interactions.py` - Missing Unique Constraint & Concurrency Crash: The `save_item` endpoint queries `SavedItem` to prevent duplicates and relies on `scalar_one_or_none()`. However, `SavedItem` lacks a `UniqueConstraint` on `(user_id, target_type, target_id)`. Concurrent requests bypass the check and insert duplicates. Any subsequent save fetch will permanently crash the endpoint with a `MultipleResultsFound` 500 error for that user.
- [ ] **[MEDIUM]** `frontend/package.json` - Bundle Bloat: Includes notoriously massive libraries (`d3`, `lucide-react`) without explicit tree-shaking configuration verified in `next.config.mjs`, likely increasing initial chunk sizes significantly.
- [ ] **[MEDIUM]** `frontend/app/(dashboard)/exam/[subjectId]/page.tsx` - React Dependency Loop: The exam countdown `setInterval` logic includes `handleSubmit` in its dependency array. `handleSubmit` relies on `answers`. Consequently, every time a user selects an answer, `handleSubmit` changes, triggering the `useEffect` to clear and reset the 1-second interval. If a user clicks answers quickly, the timer will repeatedly reset and not tick down, granting unlimited time.
- [ ] **[HIGH]** `frontend/lib/store.js` - SWR Global Cache Pollution & Privacy Leak: The `logout()` function clears the auth session but completely fails to clear the global SWR cache. Because SWR cache keys (like `/progress/sections/...`) are strictly URL-based and omit user identifiers, a new user logging in on the same device will see the previous user's cached private data (course progress, exam scores, saved items) flashed on the screen or persisted indefinitely if revalidation is disabled.
- [ ] **[HIGH]** `backend/app/models/interactions.py` - Missing Foreign Keys & Orphaned Data Risks: The `UserNote` and `SavedItem` models use `mapped_column(Integer, nullable=True)` for `topic_item_id` and `tab_content_id` without explicit `ForeignKey` constraints (`ondelete="CASCADE"` or `SET NULL`). If a `TopicItem` is deleted, these orphaned records will remain in the database pointing to non-existent IDs, eventually causing 500 crashes during frontend workspace serialization.
- [ ] **[HIGH]** `backend/app/services/xp.py` - In `update_daily_quests_progress`, the code iterates over `quest_progress.items()` and performs `await db.execute(update(DailyQuest)...)` sequentially inside a `for` loop, causing an N+1 query issue for database updates.
- [ ] **[HIGH]** `backend/app/routers/gamification.py` - In `get_subject_plan`, nested relations (chapters, lessons, blocks, quizzes) are loaded entirely into memory and iterated over using nested list comprehensions (e.g., `[l.id for c in subject.chapters for l in c.lessons]`). This will cause CPU blocking and severe memory bloat on large subjects.
- [ ] **[HIGH]** `frontend/app/(dashboard)/professor-chat/page.tsx` (and `app/professor/chat/page.tsx`) - Missing DOM Virtualization. Chat messages are rendered using a simple `messages.map()` without virtualization (`react-window` or `react-virtuoso`). For conversations with hundreds of messages, this will cause severe DOM bloat, high memory usage, and extreme scroll lag.
- [ ] **[MEDIUM]** `backend/app/routers/courses.py` - Data Over-fetching. `list_topics` uses `selectinload(Topic.sections)` and `get_topic_workspace` queries `select(TopicSection)`, loading all columns including the potentially huge `TopicSection.content` (Text column), but `content` is completely unused in the API responses (`TopicCardOut` and `TopicSectionOut`). This causes unnecessary database load and memory spikes. It should use `defer(TopicSection.content)` or `load_only()`.
- [ ] **[CRITICAL]** `frontend/app/(dashboard)/exam/[subjectId]/page.tsx` - Scalability: The Exam Mode page fetches quizzes by iterating through all lessons of a subject sequentially and making an API call (`GET /quizzes/${lesson.id}`) for each until it finds a valid quiz. This is O(N) API calls causing severe bottlenecks on subjects with many lessons.
- [ ] **[MEDIUM]** `backend/app/models/admin_audit.py` - Missing Index: `created_at` lacks an index, causing sequential scans during high-frequency professor rate-limiting queries (`count()` over time range).
- [ ] **[HIGH]** `backend/app/services/xp.py` & `backend/app/routers/gamification.py` - Race Conditions: Multiple progress tracking (LessonProgress, ContentProgress, SavedItem) and gamification points use a non-atomic "check-then-create" pattern without row-level locking or db-level unique constraints, allowing duplicate records to be created.
- [ ] **[MEDIUM]** `backend/app/models/users.py` - Missing Indexes: Filtering fields heavily used by students for live sessions (`role`, `niveau`, `filiere`) are missing indexes, leading to sequential database scans.
- [ ] **[CRITICAL]** `backend/app/routers/users.py` and `backend/app/security/passwords.py` - Synchronous CPU Blocking: `hashlib.pbkdf2_hmac` is used for password hashing (`260_000` iterations) and is executed synchronously inside the async `login` and `reset_password` routes. Because FastAPI runs on a single-threaded async event loop, a single login request will block the entire server process (for tens to hundreds of milliseconds), preventing any other concurrent API requests from being handled. It must be offloaded to a thread pool using `run_in_threadpool`.
- [ ] **[HIGH]** `backend/app/models/gamification.py` - Missing Index on Sorting Column: `UserXP.total_xp` lacks an index. The `/leaderboard` endpoint relies heavily on `.order_by(UserXP.total_xp.desc())`. Without an index, PostgreSQL must perform an expensive in-memory sort (`O(N log N)`) across the entire user base on every leaderboard request, causing high CPU usage and slow responses.
- [ ] **[HIGH]** `backend/app/routers/admin.py` - Admin Dashboard DOS: The `_breakdowns` function runs multiple `select(column, func.count()).select_from(model).group_by(column)` queries concurrently using `asyncio.gather`. On large tables, performing full table scans with grouping and counting in parallel will saturate database connections and cause severe database locking/timeouts.
- [ ] **[HIGH]** `backend/app/services/realtime_outbox.py` - Blocking Gateway/Worker: `process_realtime_outbox` processes up to 500 events using a sequential `for` loop (`for event in events: await publish_ably_message(...)`). A batch of 500 events will take ~50+ seconds in network latency, causing the `/api/internal/realtime/process-outbox` endpoint to timeout for the background cron/worker invoking it. It must use `asyncio.gather` for parallel HTTP dispatch.
- [ ] **[HIGH]** `frontend/components/animated/source-ports/**/context/*Context.tsx` - React Context Re-render Cascades: Multiple Context Providers (`ThemeContext`, `SettingsContext`) are passing un-memoized object literals directly to the `value` prop (e.g., `value={{ theme, toggleTheme }}`). Because these objects are re-created on every render of the Provider, React is forced to re-render all subscribing components and their entire subtrees, completely defeating React's memoization capabilities. The context values must be wrapped in `useMemo`.
- [ ] **[MEDIUM]** `backend/app/routers/users.py` - External API Latency in HTTP Handlers: Emails (`send_verification_email`, `send_reset_email`) are `await`-ed sequentially directly inside the `/auth/signup` and `/auth/forgot-password` route handlers. While not blocking the CPU, this forces the client's HTTP request to wait for the third-party Resend API to respond (hundreds of milliseconds) before returning a success message, creating noticeable UI lag. These should be dispatched via FastAPI's `BackgroundTasks`.
- [ ] **[MEDIUM]** `backend/app/routers/gamification.py` - Unbounded Aggregate Queries: The `/stats` endpoint computes user stats by running aggregate queries (`func.sum` on `LessonProgress.watched_seconds`, `func.count` on `QuizResult`) dynamically on every request. As these progress tables grow large, performing continuous aggregations without materialized views or cached counter columns will slowly degrade API response times.
- [ ] **[MEDIUM]** `frontend/components/animated/**/PrismSimulator.tsx` - React Performance Anti-pattern: Synchronizing props to state using `useEffect` (e.g. `setLocalIncidentAngle(incidentAngle)`) is a performance anti-pattern that triggers an immediate second render for every update. State should be derived or managed in a way that avoids redundant render cycles.
- [ ] **[LOW]** `frontend/app/(dashboard)/home/[subjectId]/page.tsx` - Missing Memoization: Heavy array operations (`allSections.filter`, `topics.reduce`, `allSections.find`) are performed directly in the render body without `useMemo`. As the course content (sections/topics) scales to hundreds of items, this will cause noticeable UI frame drops during re-renders.
- [ ] **[HIGH]** `backend/app/models/*.py` - Missing Foreign Key Indexes: Widespread missing indices on foreign keys (e.g., `Chapter.subject_id`, `Lesson.chapter_id`, `QuizQuestion.quiz_id`, `UserNote.user_id`). PostgreSQL does not automatically index foreign keys, so common JOIN operations and lookups by parent ID will trigger expensive sequential scans as the database grows.
- [ ] **[MEDIUM]** `frontend/components/TopNav.tsx` & `Sidebar.jsx` - Zustand Over-subscription: Components are subscribing to the entire `useAuthStore` via destructuring (`const { user, logout } = useAuthStore()`). This causes these global components to re-render whenever *any* part of the auth state changes (e.g., `isHydrated`), even if the specific fields they use remain identical. They should use targeted selectors.
- [ ] **[LOW]** `backend/app/routers/realtime.py` - Redundant DB Calls: The `/ably-token` endpoint performs multiple sequential database queries and rebuilds the `AccessContext` twice (once for live sessions and once for offerings). These should be optimized to share the access context and potentially be fetched in parallel.
- [ ] **[MEDIUM]** `frontend/proxy.ts` - Middleware Overhead: The Next.js middleware performs heavy operations (`randomBytes(16)`, full CSP reconstruction, and JWT decoding/verification) on every single request. This adds 10-30ms of latency to every page load and transition. These operations should be cached or optimized for the Edge runtime.
- [ ] **[LOW]** `backend/app/database.py` - Connection Pool Saturation: In production-like environments (non-Lambda), the database engine uses SQLAlchemy defaults (pool_size=5). This is highly prone to saturation under moderate concurrent traffic, causing "QueuePool limit reached" errors.
- [ ] **[LOW]** `backend/requirements.txt` - Slow JSON Serialization: The backend uses the standard library `json` module. Large payloads (course plans, quiz metadata) will cause CPU spikes and block the event loop during serialization. Switching to `orjson` is recommended.
- [ ] **[MEDIUM]** `frontend/components/KrescoMascot.tsx` & `frontend/components/zed/ZedModeOverlay.tsx` - Memory Leak: Multiple `window.addEventListener` calls are made inside `useEffect` hooks without corresponding `removeEventListener` cleanup functions. This causes event handler leaks and erratic behavior on component remounts.
- [ ] **[MEDIUM]** `frontend/hooks/useFocusEngine.ts` - Logic flaw: The focus engine forces the timer state to "paused" upon component mount if it was saved as "running", defeating the purpose of a background timer tracking focus across reloads.
- [ ] **[MEDIUM]** `backend/app/routers/payments.py` - Blocking I/O: The `stripe.Webhook.construct_event` call is synchronous and computationally expensive (cryptographic verification). Since it is not wrapped in `asyncio.to_thread`, it will block the FastAPI async event loop under high webhook volume.
- [ ] **[LOW]** `backend/app/services/auth.py` - Local Caching: The Google JWKS keys are cached in memory using a global variable `_google_jwks_cache`. In a multi-worker or Lambda environment, every instance will fetch the keys independently.
- [ ] **[LOW]** `backend/app/services/media_storage.py` - Blocking I/O: `boto3.client("s3")` is called synchronously inside `_s3_client`. When instantiated during an async request, it reads configuration, hits IMDS, and blocks the event loop.
- [ ] **[HIGH]** `backend/app/routers/users.py` & `backend/app/routers/professor.py` - Resource Exhaustion (DoS): File size validation for profile pictures and chat images (`await file.read(MAX_BYTES + 1)`) happens *inside* the route handler. FastAPI will buffer extremely large multipart uploads to disk/memory *before* the route is hit, allowing attackers to exhaust server resources. Requires a global request size limit middleware.
- [ ] **[MEDIUM]** `backend/app/routers/gamification.py` & `backend/app/routers/professor.py` - Inconsistent Timezone Handling: The presence of defensive `if value.tzinfo is None: value = value.replace(tzinfo=timezone.utc)` checks indicates that naive datetimes are leaking into the database or being passed internally, which can cause subtle offset bugs if the server's local time ever diverges from UTC.
- [ ] **[MEDIUM]** `backend/app/routers/payments.py` - Missing Idempotency Key: The `/verify-session` endpoint lacks an explicit idempotency token in the request payload. While the downstream state update is convergent, the endpoint itself is vulnerable to replay attacks executing redundant database updates.
- [ ] **[LOW]** `backend/app/routers/users.py` - PII Log Leakage: In the Google login fallback flow, `logger.exception("google_login_persistence_failed email=%s", email)` directly logs the user's email in plain text into the application logs.
- [ ] **[MEDIUM]** `backend/app/services/email.py` - Missing External Timeout: `resend_sdk.Emails.send(params)` is called inside an `asyncio.to_thread` worker. Because the Resend Python SDK (which uses `requests`) does not enforce a default timeout, an outage at the provider will hang the thread indefinitely, eventually exhausting the FastAPI thread pool.
- [ ] **[LOW]** `backend/app/dependencies.py` - Implicit Rollback: `get_db` uses a simple `yield db` followed by `await db.close()`. It lacks an explicit `try...except Exception: await db.rollback()` block, relying entirely on SQLAlchemy's connection release to rollback uncommitted transactions.
- [ ] **[LOW]** `frontend/lib/ably.ts` - Silent Async Failures: The realtime channel subscription functions use fire-and-forget Promises with `.catch(() => undefined)`, completely silencing network failures or permission errors without alerting the client or observability layers.
- [ ] **[HIGH]** `backend/app/models/*.py` - Database Schema Integrity: Found 15 instances of `ForeignKey` (specifically in `professor.py`) missing `ondelete="CASCADE"` or `ondelete="SET NULL"`. Attempting to delete a user or subject will result in an `IntegrityError` due to lingering relational constraints, breaking data cleanup pipelines.
- [ ] **[LOW]** `backend/app/routers/realtime.py` - Information Disclosure: The `get_ably_token` route catches `AblyConfigurationError` and passes `str(exc)` into the `HTTPException` detail, inadvertently leaking internal configuration paths or secret hints to the client.
- [ ] **[MEDIUM]** `frontend/app/(dashboard)/professor-chat/page.tsx` - Memory/DOM Bloat: The chat interface iterates over the `messages` array using a standard `.map()`. Without list virtualization (like `react-window`), an extensive chat history will drastically increase DOM node count, causing severe browser lag and potential OOM.
- [x] **[LOW]** `frontend/components/animated/primitives.tsx` - Layout Thrashing: Progress bars and other animated primitives use Framer Motion to animate `width` and `height` properties instead of `transform: scaleX()`. This forces the browser to recalculate layout and repaint on every frame, drastically reducing perceived framerate on mobile devices.
- [ ] **[HIGH]** `backend/app/main.py` - Cache Poisoning Risk: Authenticated API endpoints do not emit `Cache-Control: no-store, private` or `Pragma: no-cache` headers. This misconfiguration allows intermediate proxies (like Vercel Edge or Cloudflare) to potentially cache and serve personalized user data to other users.
- [ ] **[LOW]** `backend/app/services/media_storage.py` - Path Traversal: `LocalMediaStorage.put_object` uses `self.root / key` directly without sanitizing the `key` or verifying that it resolves within the `self.root` boundary. While currently mitigated by UUID generation upstream, the service implementation itself is vulnerable to directory traversal.
- [ ] **[LOW]** `frontend/app/page.tsx` - Loading State Fragility: Critical form handlers (like `saveOnboarding`) reset their loading states after try/catch blocks instead of strictly within `finally` blocks. If an unhandled exception or early return occurs within the `catch`, the UI will permanently deadlock in a loading state.
- [ ] **[MEDIUM]** `backend/app/main.py` - Resource Exhaustion: There is no global middleware restricting the size of incoming `application/json` payloads. An attacker could send a multi-gigabyte JSON payload, exhausting server memory during FastAPI's automatic Pydantic deserialization.
- [ ] **[MEDIUM]** `backend/app/rate_limit.py` - IP Spoofing / Rate Limit Bypass: The `slowapi` Limiter is initialized with the default `get_remote_address`, which blindly trusts the `X-Forwarded-For` header without validating trusted proxies. Attackers can trivially bypass rate limits by spoofing this header.
- [ ] **[MEDIUM]** `backend/app/schemas/*.py` - Schema Strictness / Mass Assignment: None of the Pydantic models use `model_config = ConfigDict(extra="forbid")`. This allows attackers to submit massive JSON payloads with arbitrary unexpected keys, leading to memory bloat and potential mass-assignment vulnerabilities if payloads are dumped directly into ORMs via `.model_dump()`.
- [ ] **[LOW]** `frontend/app/watch/[lessonId]/page.tsx` & `frontend/app/(dashboard)/exam/[subjectId]/page.tsx` - Error Boundary Fragility: Complex interactive client components (video players, quiz engines) lack localized `<ErrorBoundary>` wrappings or deeply nested `error.tsx` layouts. A localized render crash in one of these sub-components will crash the entire dashboard page structure.
- [ ] **[MEDIUM]** `backend/app/routers/professor.py` - Database Deadlocks: Explicit row locks (`.with_for_update()`) on `ProfessorChatConversation` do not use `skip_locked=True` or `nowait=True`. Concurrent requests attempting to lock the same conversation will block the database connection and the FastAPI worker indefinitely, potentially leading to connection pool exhaustion.
- [ ] **[LOW]** `.github/workflows/ci-backend.yml` - CI/CD Optimization: The backend GitHub actions pipeline uses `pip install` without utilizing GitHub's actions caching (`actions/cache` or `setup-python` cache). This drastically increases build times and CI compute costs.
- [ ] **[MEDIUM]** `backend/app/routers/admin.py` - Unhandled Asyncio Exceptions: `_gather_reads` executes multiple database queries in parallel using `await asyncio.gather(*operations)`. Because it lacks `return_exceptions=True`, a timeout or failure in any single background task will raise immediately, cancelling the others and crashing the entire Admin Dashboard overview request.
- [ ] **[LOW]** `backend/app/models/gamification.py` - Missing Integer Constraints: Fields like `total_xp` and `amount` use standard `Integer` types without database-level non-negativity constraints (`CheckConstraint(column >= 0)`). A logic bug in XP calculations could result in negative XP, corrupting user progression and leaderboards.
- [ ] **[HIGH]** `backend/app/routers/payments.py` & `backend/app/routers/users.py` - Database Connection Starvation: Several endpoints (like checkout creation, Stripe webhook verification, and email dispatch) perform slow external HTTP network I/O *while* holding an active `AsyncSession` database connection fetched from `Depends(get_db)`. Under high load, these slow external requests will exhaust the database connection pool, taking down the entire API.
- [ ] **[MEDIUM]** `backend/app/dependencies.py` - Unhandled Client Disconnects: The `get_db` generator does not catch `BaseException` (or `asyncio.CancelledError`). If a client abruptly closes their browser mid-request, FastAPI throws a `CancelledError`. Without an explicit `await db.rollback()` in a `finally` or `except` block, the SQLAlchemy connection might be returned to the pool in a "dirty" uncommitted state.
- [ ] **[CRITICAL]** `backend/app/admin/views.py` - The "God Mode" Permissions Loop: A global function `configure_power_admin_view()` iterates over all models and forcibly overwrites `view.can_create = True`, `view.can_edit = True`, and `view.can_delete = True`. This completely nullifies all specific restrictions placed on sensitive gamification and progression models, granting dangerous CUD access to the entire database.
- [ ] **[CRITICAL]** `backend/app/services/course_access.py` - Missing Access Enforcement on Child Sections: In `chapter_section_out`, the engine evaluates access directly on the child component instead of calling `decide_child()`. This completely bypasses any tier or stream locks applied to the parent `Chapter`.
- [ ] **[CRITICAL]** `backend/app/services/course_access.py` - Missing Access Enforcement on Child Lessons: In `require_lesson_access`, this function fails to inherit the parent chapter's access decision via `decide_child`, causing lessons to ignore their parent's restrictions.
- [ ] **[CRITICAL]** `backend/app/services/course_access.py` - Fail-Open Access Bypasses: The engine looks up access decisions in pre-calculated dictionaries. If the mapping is missing, it "fails-open" and exposes the premium content fully unredacted (`can_access=True`) instead of failing-secure.
- [ ] **[CRITICAL]** `backend/app/services/course_access.py` - Subject Enforcement Bypass via Orphaned Items: If a topic item's parent `topic` is missing, the fallback logic executes without supplying a `subject_id`, completely bypassing subject-level entitlements and granting access.
- [ ] **[CRITICAL]** `backend/app/schemas/courses.py` - Exam PDF Statement URL Leak: `exam_out` unconditionally maps `statement_url=exam.statement_url` to the response schema. Even if `can_access == False`, the direct URL to the paid exam PDF is fully leaked in the JSON response payload.
- [ ] **[HIGH]** `backend/app/routers/professor.py` - Conversation TOCTOU Race Condition: `start_student_conversation` checks for an existing conversation using `db.scalar(...)` before inserting a new one, without holding a DB lock. Concurrent clicks create duplicate 1-to-1 conversation threads and corrupt the data model.
- [ ] **[HIGH]** `backend/app/routers/professor.py` - Missing Authorization on Chat History Deletions: `delete_chat_message` fails to invoke `_ensure_student_professor_chat_access(user)`. A student whose subscription is expired or revoked can maliciously tamper with and delete historical chat records.
- [ ] **[HIGH]** `backend/app/routers/professor.py` - Unbounded Chat History Deletions: `delete_chat_message` lacks the 15-minute time restriction present on message edits, allowing students and professors to permanently delete messages from years ago.
- [ ] **[HIGH]** `backend/app/routers/professor.py` - Live Session State Overwrites: Mutating endpoints for live sessions fetch the `LiveSession` model without `with_for_update()`. Concurrent actions by an admin or professor will blindly overwrite state.
- [ ] **[HIGH]** `backend/app/admin/views.py` - Bypassing Atomic Business Logic via Admin Edits: Because models like `UserXP` and `DailyQuest` can be edited directly via the admin panel, staff can forcefully update progress or XP without triggering the atomic gamification logic built into the `xp.py` service.
- [ ] **[HIGH]** `backend/app/admin/views.py` - Orphaned States via Admin Deletions: Models like `User` and `QuizAttempt` can be directly deleted via the admin interface. A raw DB deletion leaves orphaned active sessions in Redis and orphans dependent gamification logs.
- [ ] **[HIGH]** `backend/app/admin/views.py` - Admin Staff Verification Vulnerability: The base `PowerModelView` fails to explicitly override `is_accessible()`. It relies entirely on a global upstream authentication configuration, lacking defense-in-depth verification for the `is_staff` role at the view level.
- [ ] **[MEDIUM]** `backend/app/routers/professor.py` - Unhandled VdoCipher Exceptions: `create_live_session` synchronously awaits `create_live_stream` without a `try/except` block. A network timeout or outage on VdoCipher's end bubbles up as an unhandled HTTP 500 error, crashing the request rather than failing gracefully.
- [ ] **[MEDIUM]** `backend/app/routers/courses.py` - Exposing Unpublished Subjects via Direct ID: The `GET /subjects/{subject_id}` endpoint retrieves subjects purely by ID, missing the `Subject.is_published == True` filter applied to list endpoints. Users can bypass UI barriers by guessing IDs and enumerate draft subjects.
- [ ] **[MEDIUM]** `backend/app/routers/courses.py` - Parent-Level Publication Ignored: Endpoints like `list_topics` and `get_exam_bank` filter for `status == "published"` on the child items, but fail to assert if the parent `Subject` itself is published, exposing active items belonging to unreleased courses.
- [ ] **[MEDIUM]** `backend/app/routers/professor.py` - Missing Pagination Bounds: Multiple endpoints query chat history and admin requests using unbounded `.all()` queries without `.limit()` or `.offset()`, leading to eventual memory exhaustion as history scales.
- [ ] **[MEDIUM]** `backend/app/routers/professor.py` - Unreachable Checkpoints (Hardcoded Limits): `list_professor_live_checkpoints` utilizes hardcoded `.limit(50)` without supporting offset or cursor parameters, meaning older checkpoints are permanently inaccessible.
- [ ] **[CRITICAL]** `frontend/components/SectionQuiz.tsx` - Exposure of Correct Quiz Answers: The `is_correct` boolean for every option is shipped directly to the client. Any student can inspect the network tab or React state to instantly see all correct answers.
- [ ] **[CRITICAL]** `backend/app/security/csrf.py` - Admin CSRF Bypass: The `_is_admin_path` early exit explicitly skips CSRF token validation for the `sqladmin` panel, relying entirely on easily spoofable `Origin` headers.
- [ ] **[CRITICAL]** `backend/app/routers/users.py` - Stateless Zombie Sessions: The `/auth/logout` endpoint deletes the client cookie but fails to increment the `auth_token_version` in the database. The 7-day JWT remains 100% valid on the server even after the user logs out.
- [ ] **[HIGH]** `backend/app/routers/courses.py` - Unauthenticated Metadata Access: `GET /api/courses/subjects` and `GET /api/courses/chapters/{chapter_id}` lack the `Depends(get_current_user)` dependency, leaking the entire structural curriculum to unauthenticated requests.
- [ ] **[HIGH]** `frontend/proxy.ts` - Fake Client Route Protection: Next.js middleware relies on decoding unverified JWT payloads and cookies to enforce route protection. Clients can easily spoof these to view restricted frontend code.
- [ ] **[CRITICAL]** `backend/app/routers/users.py` - Race Conditions (User Creation): User `signup` and `google_login` use an `if not exists -> select -> insert` pattern without `with_for_update()` locking or `ON CONFLICT` constraints, leading to 500 crashes under concurrent requests.
- [ ] **[CRITICAL]** `frontend/app/layout.tsx` - Global Dynamic Rendering: Indiscriminate use of `await connection()` forces the entire application to render dynamically on every request, completely destroying Static Site Generation (SSG) for static pages.
- [ ] **[MEDIUM]** `frontend/app/layout.tsx` - Uncompressed Fonts: Loading uncompressed `.otf` files (`sf-pro-rounded`) instead of optimized `.woff2` causes severe FOUT/FOIT issues and inflates the initial bundle size.
- [ ] **[HIGH]** `frontend/components/zed/PdfViewer.tsx` - Aggressive IndexedDB Blob Storage: Loads and stores entire PDF files as `Blob` objects in IndexedDB. This will cause mobile browsers to quickly hit quota limits and stutter from heavy Garbage Collection.
- [ ] **[MEDIUM]** `frontend/components/zed/ZedModeOverlay.tsx` - Blocking Main Thread: Widespread synchronous use of `localStorage` blocks the main thread during component mounting, severely delaying React hydration.
- [ ] **[CRITICAL]** `.github/workflows/ci-backend.yml` - Fake Database Tests: CI spins up a Postgres container, but the tests are hardcoded to test against an in-memory SQLite database (`sqlite+aiosqlite:///:memory:`). Postgres-specific indexing or JSONB logic will fail in production but pass CI.
- [ ] **[CRITICAL]** `frontend/playwright.integration.config.ts` - Real-Time Code is Untested: E2E testing explicitly sets `NEXT_PUBLIC_ABLY_ENABLED='false'`, meaning core features like live chats and real-time notifications are entirely untested.
- [ ] **[HIGH]** `backend/requirements.txt` & `frontend/package.json` - Zero Test Coverage: `pytest-cov` is missing, and Vitest runs without coverage flags. It is impossible to know how much of the codebase is actually covered.
- [ ] **[MEDIUM]** `.github/workflows/` - CI Thrashing: Github Actions do not cache Python dependencies or Playwright Chromium binaries, leading to incredibly slow, resource-heavy builds on every commit.
- [ ] **[CRITICAL]** `backend/app/schemas/courses.py` - Quiz answers still leak via `TabContent.config_json`. `_scrub_quiz_data` only scrubs `ChapterSection.quiz_data`. Topic-item quiz tabs serialize `config_json` verbatim — answer, accepted_answers, answerRegion, is_correct all reach the browser for accessible content.
- [ ] **[CRITICAL]** `backend/main.py` & `frontend` - No APM or error tracking. Zero Sentry, Datadog, OpenTelemetry, or CloudWatch metrics. Debugging production failures means grepping unstructured text logs, meaning student-impacting bugs may go unnoticed for hours.
- [ ] **[HIGH]** `backend/app/admin/views.py` - Admin User view allows staff to edit `is_staff`/`is_superuser`/`role` (`admin/views.py:91-104`). This allows a basic staff member to arbitrarily escalate their privileges to superuser.
- [ ] **[HIGH]** `.github/workflows/deploy-backend.yml` - Production deploys run `alembic upgrade head` on the live DB with no rollback safety. If a migration breaks during a deployment, the database will be left in a corrupted intermediate state.

## 🔍 Round 2 — Deep Audit Findings (Previously Unaudited Areas)

- [ ] **[CRITICAL]** `backend/app/routers/realtime.py:82-102` & `backend/app/services/access.py:101-106` - Ably Capability Escalation: A student with `live_sessions` feature granted but `basic` tier and zero entitlement rows bypasses subject scoping (`subject_scope_enforced=False`), receiving Ably subscribe capability for ALL course offerings in their track — including subjects they never paid for.
- [ ] **[CRITICAL]** `backend/app/models/gamification.py:36-48` - Missing UniqueConstraint on `ContentProgress(user_id, item_type, item_id)`. Only a non-unique index exists. Two concurrent complete requests both find `None` and both insert, creating duplicates. Causes inflated progress percentages, double-counted lesson stats, and `scalar_one_or_none` raises 500.
- [ ] **[CRITICAL]** `backend/app/models/interactions.py:65-78` - Missing UniqueConstraint on `SavedItem(user_id, target_type, target_id)`. Concurrent saves create duplicate rows; subsequent `scalar_one_or_none` reads raise `MultipleResultsFound` (500 error) once duplicates accumulate.
- [ ] **[CRITICAL]** `backend/app/routers/quizzes.py:71-118` & `backend/app/models/gamification.py:94-108` - `QuizResult` has no `UniqueConstraint(user_id, quiz_id)`. `with_for_update()` locks zero rows for first-time submissions. Two concurrent submits both insert duplicate result rows. Combined with duplicate `DailyQuest` rows (different IDs), XP idempotency key differs per row so XP can be double-granted.
- [ ] **[HIGH]** `backend/app/models/gamification.py:153-175` - Missing UniqueConstraint on `TopicItemProgress(user_id, topic_item_id)`. Concurrent start/complete requests create duplicates; routes using `scalar_one_or_none` will raise 500.
- [ ] **[HIGH]** `backend/app/models/gamification.py:111-128` - Missing UniqueConstraint on `DailyQuest(user_id, quest_type, date)`. Two concurrent first-of-day requests both insert the full 3-quest template set → 6 quests. `UPDATE progress = progress + N` increments ALL matching rows, doubling daily-quest XP.
- [ ] **[HIGH]** `backend/app/models/gamification.py:15-33` - Missing UniqueConstraint on `LessonProgress(user_id, lesson_id)`. Read-then-insert pattern creates duplicates under concurrency, corrupting watched_seconds and completion status counts.
- [ ] **[HIGH]** `backend/app/services/xp.py:156-170` - `award_xp` first-award path not concurrency-safe. When no `UserXP` row exists, two concurrent first-time awards both INSERT. `UserXP.user_id` has `unique=True`, so the second raises `IntegrityError`, aborts the transaction, and the user silently loses their XP. `award_xp_bulk` at `:295-296` has the same flaw.
- [ ] **[HIGH]** `backend/app/routers/gamification.py:632-656` - Daily-quest claim race condition. `claim_daily_quest` reads quest, checks `if quest.completed`, then sets it with no `with_for_update()`. Two concurrent claims both pass the check. Combined with duplicate quest rows (different IDs), XP idempotency keys differ and XP is double-granted.
- [ ] **[HIGH]** `backend/app/routers/users.py:141,146` - Google login email not normalized before lookup/insert. Password signup normalizes to `.lower().strip()` but Google login uses raw `payload["email"]`. A Google identity differing only in case creates a second `User` row, splitting the user's XP/progress and granting a second verified account.
- [ ] **[HIGH]** `backend/app/routers/calendar.py:104-115` - Calendar access-control bypass. Niveau/filiere filtering is only applied when `user.role == "student"`. Any authenticated user with a non-"student" role (null role, legacy value, etc.) reads every calendar event including live-session `join_url` across all tracks — leaking paid gated content.
- [ ] **[HIGH]** `backend/app/routers/payments.py:80-85` - Stripe webhook grants Pro without checking `payment_status == "paid"`. `checkout.session.completed` fires for async/pending states where `payment_status` is `"unpaid"` or `"no_payment_required"`, granting Pro access before money is captured.
- [ ] **[HIGH]** `frontend/components/VideoPlayer.jsx:142-223` - Stale-closure bug: player never re-attaches when `lessonId` changes. Init effect does not depend on `lessonId`. If `streamData` for two lessons is referentially equal (cached response), the effect does not re-run and progress/XP completion fires against the wrong lesson.
- [ ] **[MEDIUM]** `backend/app/routers/payments.py:87-88` - `invoice.payment_failed` permanently revokes Pro on first transient Stripe retry. Stripe fires this during the dunning grace period while the subscription is still active. Legitimate paying users lose access on a momentarily-declined card. Only `customer.subscription.deleted` should trigger revocation.
- [ ] **[MEDIUM]** `backend/app/routers/payments.py:58-105` - Stripe webhook has no event idempotency/dedup. Processed `event.id` is never stored. Stripe delivers events at-least-once. Out-of-order or duplicate `revoke` events can clobber a user who has since re-subscribed.
- [ ] **[MEDIUM]** `backend/app/services/xp.py:258-264` - `award_xp_bulk` raises on duplicate idempotency keys within one batch. `INSERT ... ON CONFLICT DO NOTHING` does not deduplicate rows within the same VALUES list. Two awards sharing a non-null `idempotency_key` in one batch abort the entire batch.
- [ ] **[MEDIUM]** `backend/app/models/gamification.py:71` & `backend/app/services/xp.py` - `XPTransaction.idempotency_key` unique index is global, not per-user. `award_xp_bulk`'s `on_conflict_do_nothing(index_elements=["idempotency_key"])` uses the global index only. Any key not scoped by user_id in the string would suppress XP for the wrong user.
- [ ] **[MEDIUM]** `backend/app/routers/users.py:371-380` - Reset-password error codes leak account existence. Returns `404` when the user doesn't exist vs `400` for a stale token, allowing attackers to distinguish non-existent accounts from stale-token states.
- [ ] **[MEDIUM]** `backend/app/services/email.py:22-30` & `backend/app/routers/users.py:244` - Verification token never invalidated and not single-use. Unlike reset tokens, verification links work repeatedly for 24h with no `token_version` binding. A leaked link can re-trigger verification side effects.
- [ ] **[MEDIUM]** `backend/app/routers/calendar.py:21-22` - Calendar time window uses UTC `time.max` regardless of user timezone. Students in UTC+1 (Morocco) querying "this week" get a window shifted by an hour, silently dropping live sessions at the week boundary.
- [ ] **[MEDIUM]** `backend/app/routers/notifications.py:39-60` - `DELETE /notifications` is an unconfirmed mass-delete with no confirmation token, no dedicated rate-limit, and relies on CSRF protection currently dead (middleware misnamed). A single CSRF-able request wipes all notifications.
- [ ] **[MEDIUM]** `backend/app/models/*.py` - Pervasive missing `server_default` on boolean/int columns. `User.is_active`, `User.is_staff`, `User.auth_token_version`, `Notification.is_read`, `UserXP.total_xp`, `DailyQuest.completed`, `RealtimeOutbox.status`/`attempts` have Python-only `default` but no `server_default`. Raw SQL, bulk INSERT, or admin-created rows get NULL. NULL `is_active` silently locks out the user; NULL `status` on outbox rows means they are invisible to the `status='pending'` worker query.
- [ ] **[MEDIUM]** `frontend/components/VideoPlayer.jsx:87-96,225-246` - `saveProgress` cross-lesson contamination. The 30s interval can post the old lesson's `currentTime` against the new `lessonId` during rapid navigation, writing wrong watched-seconds to the database.
- [ ] **[MEDIUM]** `frontend/hooks/useFocusEngine.ts:106-119` - Focus engine clobbers concurrent tabs. All state writes to one global `localStorage` key with no `storage`-event coordination. A second open tab silently diverges and the last unmount wins, corrupting focus streaks and tab-warning counts (the anti-cheat mechanism).
- [ ] **[MEDIUM]** `frontend/components/zed/PdfViewer.tsx:365-371` - PDF iframe uses a `blob:` URL that inherits the app's own origin. The sandbox only sets `allow-downloads`; a crafted file sniffed as HTML by the browser could execute script same-origin to the app (accessing localStorage, auth cookies). Only extension/MIME is validated, not magic bytes (`%PDF-`).
- [ ] **[LOW]** `backend/app/models/quizzes.py:80,88` - `Question.external_id` defaults to `""` with a `UniqueConstraint(question_set_id, external_id)`. Creating two questions in the same set without an external_id both default to `""` → `IntegrityError`. Legitimate multi-question authoring without external IDs is broken.
- [ ] **[LOW]** `backend/app/models/professor.py:187-188` - `ProfessorChatConversation.unread_for_professor` and `unread_for_student` have no `server_default` and no `CheckConstraint(>= 0)`. A decrement bug drives them negative without a DB-level guard.
- [ ] **[LOW]** `backend/app/routers/notifications.py:99-102` - `mark_notification_read` always writes and commits even when `is_read` is already true, generating unnecessary WAL churn on every client poll.
- [ ] **[LOW]** `backend/app/routers/quizzes.py:83-89` - `submit_quiz` returns the freshly-computed (possibly failing) score for the current attempt when `existing_result.passed` is true, not the stored historical pass. A student who already passed can see an incorrect "failed" score.
- [ ] **[LOW]** `frontend/app/watch/[lessonId]/page.tsx:120-139` & `frontend/lib/watchViewModel.ts:62-64` - Watch-page notes, Zed scratchpad (`kresco_zed_scratchpad`), and pins (`kresco_zed_pins`) are stored in `localStorage` keyed by content ID only, not by user. On a shared device (school/cybercafé — Kresco's target market), user B can read and edit user A's private study notes.
- [ ] **[LOW]** `frontend/app/(dashboard)/topics/[topicId]/page.tsx:343-362` - `CommentsTab` has no `AbortController`. Rapidly switching topic items leaves overlapping in-flight requests; a slow earlier response can set comments for the wrong item.
- [ ] **[LOW]** `frontend/app/(dashboard)/home/[subjectId]/page.tsx:67-116` - Subject load effect has no request cancellation. Switching `subjectId` quickly applies stale data from the previous subject to the new view.
- [ ] **[MEDIUM]** `backend/app/models/courses.py` - Missing Eager-Loading Defaults: Most relationships use default `lazy='select'`. Highly vulnerable to N+1 regressions if `selectinload()` is forgotten in new endpoints.
- [ ] **[MEDIUM]** `backend/app/routers/courses.py` - Missing Compound Index in `list_topics` on `(user_id, topic_item_id, status)` for `TopicItemProgress`, causing slow range scans.
- [ ] **[HIGH]** `backend/app/routers/professor.py` - Unindexed Rate Limiter in `_enforce_professor_mutation_rate_limit`. `AdminAuditLog` is queried by `note` and `request_path` without indexes, requiring expensive table scans as the table grows.
- [ ] **[LOW]** `backend/app/routers/courses.py` - In-Memory Filtering in `get_topic_workspace`. Search filtering (`_matches_item`) is executed entirely in Python memory after pulling all resources from DB, instead of DB-level text search.
- [ ] **[LOW]** `frontend/components/Leaderboard.tsx` - Suboptimal Local Search & Filtering. Modifying `searchInput` causes a cascade of expensive array regenerations, and it only filters the currently loaded page instead of hitting the backend.
- [ ] **[LOW]** `frontend/components/Leaderboard.tsx` - Missing Memoization. `LeaderboardRow` and `AvatarBubble` are not wrapped in `React.memo()`, causing full VDOM re-renders on keystrokes.
- [ ] **[MEDIUM]** `frontend/components/activities/InteractiveActivityRenderer.tsx` - Partial Lazy Loading. Standard activities are imported synchronously at the top level, bloating the JS bundle for every activity type even if not needed.
- [ ] **[MEDIUM]** `backend/requirements.txt` - Dependency Risk. Lacks a lockfile mechanism (no hashes, no explicit transitive versions). Exposes the application to supply-chain attacks. Use `pip-tools` or `Poetry`.
- [ ] **[LOW]** `.github/workflows/deploy-backend.yml` - Environment Drift. Testing tools like `zappa` and `pytest` are hardcoded and `pip install`ed directly inside the workflow rather than tracked in `requirements-dev.txt`.
- [ ] **[LOW]** `.github/workflows/ci-frontend.yml` - Missing Frontend Caching. Does not cache the Next.js build output (`.next/cache`), slowing down frontend build times during CI/CD.
- [ ] **[HIGH]** `frontend` - SEO & Metadata. Next.js Metadata API is underutilized. Pages are Client Components using `useEffect` for title, defeating SSR SEO. Missing Open Graph and Twitter Card tags in root layout.
- [ ] **[MEDIUM]** `frontend/components/TopNav.tsx` - Accessibility. Missing `aria-label` and `aria-expanded` on critical interactive elements like the User Menu. Decorative SVGs missing `aria-hidden="true"`.
- [ ] **[LOW]** `frontend` - UI/UX & Localization. Hardcoded mixed-language strings (English and French intermixed). Missing localized strings structure.
- [ ] **[LOW]** `frontend/app/page.tsx` & `frontend/app/pricing/page.tsx` - Accessibility. Missing proper landmark tags (e.g. `<main>`).
- [ ] **[HIGH]** `frontend/app/admin/courses/page.tsx` & `frontend/components/Leaderboard.tsx` & `frontend/components/VideoQuizOverlay.tsx` - Swallowed Exceptions. API errors are caught and swallowed using `.catch(() => {})`, hiding potentially critical failures from the user and breaking error observability.
- [ ] **[HIGH]** `frontend/app/(dashboard)/home/[subjectId]/page.tsx` - Masked Promise Errors. `fetchSubjectPlan` and `topics` endpoints use `.catch(() => null)` inside a `Promise.all` block. This silently masks network/API failures, causing the page to incorrectly display empty course structures instead of triggering an error boundary or retry state.
- [ ] **[MEDIUM]** `frontend/lib/store.js` - Ignored Logout Failures. The `/api/auth/logout` fetch call uses `.catch(() => {})`, ignoring network failures. If the network drops, the backend token remains active while the frontend state clears, leaving an orphaned active session.
- [ ] **[MEDIUM]** `frontend/components/AuthGuard.jsx` - Hydration Mismatch Risk. Uses `typeof window !== 'undefined' ? window.location.pathname : ''` directly inside the render cycle (in `AccessDeniedScreen` `href` prop). This causes a hydration mismatch error since the SSR output (`''`) will differ from the client's initial render (`/some-path`).
- [ ] **[HIGH]** `backend/app/models/gamification.py` - Missing Foreign Keys and Indexes. Tracking columns like `topic_section_id`, `topic_item_id`, `tab_content_id`, `question_id`, `question_set_id` in `XPTransaction`, `QuizAttempt`, and `QuestionAttempt` are defined as `mapped_column(Integer, nullable=True)` without explicit `ForeignKey` constraints or `index=True`. This leads to severe N+1 scanning issues when filtering analytics by these columns and poses a data integrity risk if parent elements are deleted.
- [ ] **[MEDIUM]** `backend/app/routers/admin.py` - Admin Dashboard Unbounded Parallel Aggregations: `get_admin_overview` runs over 40 database aggregate queries (`func.count`, `func.sum`) via `asyncio.gather(*operations)`. Certain operations, like `_breakdown`, execute full table scans and `GROUP BY` on massive tables (e.g., `User`, `ActivityEvent`, `Notification`). Even with a semaphore of 8, this can heavily saturate database connections and CPU under load.
-   [   ]   * * [ H I G H ] * *   \  a c k e n d / a p p / c o n f i g . p y \   -   T h e   a p p l i c a t i o n   u s e s   \ F A L L B A C K _ J W T _ S E C R E T \   b y   d e f a u l t   a n d   s t a r t s   u p   p e r f e c t l y   f i n e   i n   n o n - p r o d u c t i o n   e n v i r o n m e n t s   w i t h   a   h a r d c o d e d   J W T   s e c r e t   ( \  a l l b a c k - s e c r e t - c h a n g e - i n - p r o d u c t i o n \ ) .   W h i l e   i t   e r r o r s   i n   p r o d u c t i o n   e n v i r o n m e n t s ,   r u n n i n g   t h e   a p p l i c a t i o n   i n   o t h e r   e n v i r o n m e n t s   ( e . g . ,   s t a g i n g )   e x p o s e s   i t   t o   t o k e n   f o r g e r y   s i n c e   t h e   s e c r e t   i s   p u b l i c l y   k n o w n .  
 -   [   ]   * * [ H I G H ] * *   \  a c k e n d / a p p / r o u t e r s / u s e r s . p y \   -   P r e - A c c o u n t   T a k e o v e r   v i a   u n v e r i f i e d   e m a i l   s q u a t t i n g .   T h e   s y s t e m   a l l o w s   o v e r w r i t i n g   a n   u n v e r i f i e d   a c c o u n t ' s   p a s s w o r d .   H o w e v e r ,   p r e v i o u s l y   g e n e r a t e d   e m a i l   v e r i f i c a t i o n   t o k e n s   a r e   N O T   i n v a l i d a t e d   ( t h e y   o n l y   e n c o d e   t h e   e m a i l   w i t h o u t   a   v e r s i o n   o r   h a s h   c h e c k ) .   I f   a n   a t t a c k e r   s i g n s   u p   w i t h   t h e   v i c t i m ' s   e m a i l ,   t h e   a t t a c k e r ' s   p a s s w o r d   i s   s e t .   I f   t h e   v i c t i m   h a p p e n s   t o   c l i c k   A N Y   p r e v i o u s l y   s e n t   v e r i f i c a t i o n   l i n k ,   t h e   a c c o u n t   i s   v e r i f i e d   b u t   t h e   p a s s w o r d   r e m a i n s   t h e   a t t a c k e r ' s ,   a l l o w i n g   t h e   a t t a c k e r   t o   a c c e s s   t h e   v i c t i m ' s   v e r i f i e d   a c c o u n t .  
 -   [   ]   * * [ M E D I U M ] * *   \  a c k e n d / a p p / r o u t e r s / u s e r s . p y \   &   \  a c k e n d / a p p / s e c u r i t y / c s r f . p y \   -   I n s e c u r e   \ s a m e s i t e = \  
 n o n e \ \   c o n f i g u r a t i o n .   I n   p r o d u c t i o n   m o d e ,   b o t h   \ k r e s c o _ t o k e n \   a n d   \ k r e s c o _ c s r f \   c o o k i e s   a r e   s e t   w i t h   \ s a m e s i t e = \ n o n e \ \ .   W h i l e   t h e   D o u b l e   S u b m i t   C o o k i e   p a t t e r n   i s   u s e d   t o   m i t i g a t e   C S R F ,   s e t t i n g   \ s a m e s i t e = \ n o n e \ \   f o r   t h e   m a i n   a u t h e n t i c a t i o n   t o k e n   u n n e c e s s a r i l y   e x p o s e s   t h e   s e s s i o n   t o   c r o s s - s i t e   r e q u e s t   a t t a c k s   i f   C O R S   i s   e v e r   s l i g h t l y   m i s c o n f i g u r e d   o r   i f   t h e r e ' s   a   r e l a t e d - d o m a i n   a t t a c k   ( e . g . ,   s u b d o m a i n s ) .   I t   s h o u l d   b e   s e t   t o   \ l a x \   o r   \ s t r i c t \   i f   t h e   A P I   a n d   f r o n t e n d   s h a r e   t h e   s a m e   s i t e .  
 