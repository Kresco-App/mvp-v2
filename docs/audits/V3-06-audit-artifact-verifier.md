# Audit Artifact Verification
## Summary
- `git status --short` was run first. The tree has broad source/test/config WIP plus untracked `docs/audits/`, so every finding below is tagged `[WIP-PROVISIONAL]`.
- `docs/audits/` contains the master report, state file, 10 Wave 1 reports, 10 Wave 2 reports, and `patches/09-devops-workflows.patch`. The master report enumerates Wave 1 and Wave 2 report filenames accurately. `_state.md` enumerates Wave 1 and Wave 2 agents accurately, but its "Audit artifacts added" list is incomplete.
- Master/state do not falsely claim fixpoint completion: both say fixpoint/final verification is not complete. However, their status and pending-lead text is stale after the fix pass and would mislead a final presentation.
- `git diff --check` was rerun and matched the recorded result: exit 0 with line-ending warnings only. No saved raw output for the recorded pytest, `npm run typecheck`, or `npm run lint` runs was found in `docs/audits/`; current files contain the named regression tests and code fixes, but those pass counts were not independently rerun here.
## Findings - severity, exact file:line, quoted evidence, concrete fix
- HIGH `[WIP-PROVISIONAL]` - The master report still tells readers to apply an obsolete DevOps patch that Wave 2 explicitly invalidated.
  Evidence: `docs/audits/00-MASTER-REPORT.md:25-26` says "Several obvious DevOps workflow fixes have an apply-ready patch artifact at `docs/audits/patches/09-devops-workflows.patch`." `docs/audits/00-MASTER-REPORT.md:59-62` says "HIGH - Apply and verify the DevOps workflow patch" and "Action: apply `docs/audits/patches/09-devops-workflows.patch` after local verification." The same file later says at `docs/audits/00-MASTER-REPORT.md:140-143` that the patch is incomplete and "Do not apply the patch as-is."
  Concrete fix: Replace the executive-summary and backlog wording with "Wave 1 patch is superseded/obsolete; do not apply as-is." Either replace the patch artifact with a corrected patch or remove it from final actionable artifacts.

- HIGH `[WIP-PROVISIONAL]` - Master/state status text is internally stale about the fix pass.
  Evidence: `docs/audits/00-MASTER-REPORT.md:4-7` says fixpoint criteria are not met because "obvious fixes have not yet been implemented," but `docs/audits/00-MASTER-REPORT.md:174-175` says "Fix Pass Results" and "Implemented in the working tree after Wave 2." `_state.md` repeats the stale status at `docs/audits/_state.md:210-214`, including "obvious code fixes are pending."
  Concrete fix: Update the status and fixpoint checklist to say the obvious fix pass has run, while fixpoint remains `no` because final verification has not completed and external/product/MEDIUM+ leads remain.

- HIGH `[WIP-PROVISIONAL]` - `_state.md` leaves completed fix-pass items in the pending queue.
  Evidence: `docs/audits/_state.md:143-148` says bearer logout revocation, `token_type`, OpenAPI export, and mixed bearer/cookie CSRF remain. `docs/audits/_state.md:158-167` says to implement critical tests plus public mutation response models/internal route hiding. `docs/audits/_state.md:204-206` says to verify concept wildcard escaping and query length. But `_state.md:90-131` records those as completed and validated, and current files show matching implementations such as `backend/app/routers/internal.py:15` with `include_in_schema=False`, `backend/app/routers/exam_bank.py:68-82` with 30/minute progress limits, `backend/app/routers/exercises.py:34` with `max_length=80`, and `backend/tests_fastapi/test_auth.py:735-745` with bearer logout revocation coverage.
  Concrete fix: Reconcile "Pending Leads After Wave 2" after the fix pass. Remove completed work from pending, retain only still-open items such as mixed bearer-plus-cookie CSRF policy, OpenAPI export ownership if still unresolved, resource-open/error-envelope decisions, external Firestore/cloud checks, and CMI documentation verification.

- MEDIUM `[WIP-PROVISIONAL]` - `_state.md` marks exam iframe click/viewport gating as resolved even though the referenced current file still creates the iframe immediately.
  Evidence: `docs/audits/_state.md:81-82` lists "Event-only toaster loading and exam iframe click/viewport gating" under resolved add-now fixes. `docs/audits/W2-08-frontend-performance-deepening.md:49-63` says the exam correction iframe issue remains and recommends rendering no iframe `src` until explicit click. Current code still renders an immediate iframe at `frontend/components/figma/workspace.tsx:94-96`: `<iframe`, `title="Kresco lesson video"`, `src={iframeSrc}`.
  Concrete fix: Either implement the exam correction video gate and update the fix-pass changed-file list, or remove exam iframe gating from the resolved list and keep it as a pending performance backlog item. The master fix-pass results should say only Sonner was fixed if the iframe work remains open.

- MEDIUM `[WIP-PROVISIONAL]` - The fix-pass workflow output list does not match current working-tree evidence.
  Evidence: `_state.md:117-123` lists six changed workflow files: `ci-backend.yml`, `ci-frontend.yml`, `deploy-backend.yml`, `deploy-frontend.yml`, `deploy-staging.yml`, and `stop-staging-cloud-sql-nightly.yml`. `git status --short .github/workflows docs/audits` showed only `M .github/workflows/deploy-backend.yml` plus untracked `docs/audits/`. `git diff -- .github/workflows/deploy-backend.yml` showed only the production `backend_min_instances` branch/min-instances change.
  Concrete fix: Split "currently present in workflow files" from "changed by this fix pass." If the final artifact is meant to describe current WIP, list only `.github/workflows/deploy-backend.yml` as a workflow change and note that other workflow behaviors were already present in `HEAD/current files`, or reapply the intended workflow edits so the working tree matches the state.

- LOW `[WIP-PROVISIONAL]` - `_state.md`'s "Audit artifacts added" list omits all Wave 2 report artifacts.
  Evidence: `docs/audits/_state.md:11-24` lists only the master, state, Wave 1 reports, and `patches/09-devops-workflows.patch`. The same file enumerates Wave 2 report paths at `docs/audits/_state.md:49-73`, and the directory listing includes `W2-01-firestore-realtime-auth.md` through `W2-10-backend-security-followup.md`.
  Concrete fix: Add the 10 Wave 2 reports and this verifier report to "Audit artifacts added" before final presentation.

- LOW `[WIP-PROVISIONAL]` - Validation summaries are not fully traceable from saved artifacts.
  Evidence: `docs/audits/00-MASTER-REPORT.md:210-219` and `docs/audits/_state.md:125-131` record pass/fail summaries for pytest, frontend typecheck, frontend lint, and `git diff --check`, but they do not include exact pytest node IDs, command transcripts, timestamps, or log paths. Only `git diff --check` was independently rerun here and matched the recorded exit-0 result with line-ending warnings.
  Concrete fix: Add exact command strings and short output excerpts, or rerun the recorded commands immediately before final presentation and update master/state with V3-verified results. Keep the broader backend order-sensitive failure explicitly separated from fix-pass validation.
## Leads - remaining questions or `None`
- Need either saved command output or a fresh rerun for the recorded pytest, `npm run typecheck`, and `npm run lint` pass claims if final presentation requires validation provenance stronger than current-file inspection.
