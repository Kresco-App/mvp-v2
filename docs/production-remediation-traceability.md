# Production Remediation Traceability

This document is the working launch gate for the production hardening effort. It maps each audited blocker to an implementation slice, evidence required for completion, and current status. Do not use it as proof of readiness by itself; each item needs code, tests, or runtime evidence.

## Status Legend

- `blocked`: cannot launch until resolved.
- `in_progress`: implementation has started but the acceptance evidence is incomplete.
- `pending`: accepted requirement, not started.
- `verified`: current-state evidence proves the requirement.

## P0 Production Freeze

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| OPS-FREEZE-001 | Production deploys could be triggered before the remediation launch gate is complete. | Remove automatic production deploy triggers and require an executable production launch-gate check before any production backend or frontend deploy. | Deploy workflows are manual-only, production deploy paths run `scripts/check_production_launch_gate.py`, and tests prove the current repo fails closed while traceability rows or readiness score remain incomplete. | verified |

## P1 Security Boundary

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| SEC-CSRF-001 | HttpOnly auth cookies with `SameSite=None` allow cross-site unsafe requests unless guarded. | Enforce trusted `Origin`/`Referer` plus signed double-submit CSRF token for unsafe cookie-authenticated requests. Exempt only explicit auth bootstrap and vendor webhook paths. | Backend middleware/service, frontend header propagation, tests for allowed same-origin writes, missing token denial, hostile-origin denial, bearer-token compatibility, and webhook exemption. | verified |
| SEC-CSP-001 | HttpOnly cookies reduce token theft but do not stop an XSS payload from issuing same-origin credentialed writes. | Add a blocking frontend CSP with nonce-gated scripts, no inline script allowance, and explicit third-party sources for Google auth, VdoCipher, YouTube, uploaded media, and Ably. | Next proxy CSP implementation, dynamic App Router rendering for nonce injection, tests proving CSP on allowed and redirect responses, and production build evidence. | verified |
| SEC-CSP-STYLE-001 | The CSP still permits inline styles because the frontend has widespread inline style usage. This weakens the policy and makes future CSP tightening harder. | Move inline styles to class-based or nonce-compatible styling paths, then remove or sharply isolate `style-src 'unsafe-inline'`. | Browser production build run across core routes with CSP violation monitoring, plus tests rejecting broad inline style allowance after migration. | verified |
| SEC-ADMIN-001 | SQLAdmin uses one shared `ADMIN_PASSWORD` instead of DB-backed staff identity. | Move SQLAdmin authentication to staff users with audit trail and revocation path; remove the shared password launch dependency. | Existing `users` staff/password/session-revocation fields, admin auth tests, operational notes for granting/revoking access, and deploy config without `ADMIN_PASSWORD`. | verified |
| SEC-LIVE-001 | Live ingest URL and stream key are returned in broad professor DTOs. | Remove secrets from list/dashboard DTOs and expose via a dedicated reveal action with `no-store` and audit logging. | Schema tests proving default DTO redaction, reveal endpoint tests, audit row tests. | verified |
| SEC-SECRETS-001 | Local production-shaped secrets may exist in ignored env files. | Rotate any real local credentials and keep deploy secrets only in AWS/Vercel secret stores. | Redacting secret scanner, CI/deploy enforcement, local ignored-env scan before release, completed rotation checklist, and provider-side deploy env evidence. | in_progress |

## P2 Media Storage

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| MEDIA-S3-001 | Profile and professor-chat uploads write to local `media/` and are served publicly. | Replace local writes with a storage service abstraction backed by private S3 in staging/production. | Upload route tests using mocked storage, staging Lambda upload verification, no private chat/profile reads through public `/media`. | in_progress |
| MEDIA-AUTH-001 | Uploaded student/professor media lacks authenticated read boundary and lifecycle policy. | Generate short-lived presigned GET URLs or backend-authenticated media URLs; add object key scoping, quotas, and lifecycle cleanup. | Tests for scoped URLs and unauthorized access denial; documented S3 lifecycle rule. | in_progress |

## P3 Realtime

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| RT-FANOUT-001 | Live notifications fan out per student and each publish opens a fresh HTTP client. | Move class-wide events to offering/session channels and centralize publish client usage. | Tests proving one broadcast event for class notification; `scripts/check_staging_realtime_fanout.py --mode fanout-50 --require-provider-delivery` output from a real 50-student staging run. | in_progress |
| RT-OUTBOX-001 | Ably publish failures are logged/returned but not durable. | Add transactional `realtime_outbox`, retry/backoff, dead-letter status, and an EventBridge/SQS worker path. | Migration, service tests for claim/idempotency/retry, worker endpoint auth, scheduled worker config, bounded staging probe output, staging EventBridge schedule proof, operational runbook. | in_progress |

## P4 Backend-Backed E2E

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| E2E-STUDENT-001 | Existing Playwright smoke mostly mocks `/api/**`. | Add backend-backed student journey: login, home, topic, watch, progress. | Playwright integration test against isolated DB without internal API blanket mocks. | verified |
| E2E-PROF-001 | Professor live/chat flows are not proven through browser plus backend. | Add backend-backed professor live and chat journeys. | Playwright integration tests proving professor action becomes student-visible. | verified |
| E2E-UPLOAD-001 | Upload flows are covered by backend tests but not by a backend-backed browser journey against S3 mock/localstack or staging. | Add Playwright or staging-backed upload coverage for profile and professor/student chat media. | Browser upload test using S3 mock/localstack, or staging Lambda proof with private S3 object references and presigned read URLs. | verified |
| E2E-NEGATIVE-001 | Expired token, forbidden, backend 500, and empty states are not proven through backend-backed browser flows. | Add targeted Playwright coverage for auth expiry, forbidden access, backend failure UI, and empty states. | Backend-backed tests with controlled API/server failures and assertions for non-blank retry/error/forbidden/empty UI. | verified |

## P5 Backend Performance

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| PERF-TOPIC-001 | `get_topic_workspace` loads a broad object graph and filters in Python. | Add query-count tests, replace with targeted SQL where measured, and require hot-path composite indexes to be backed by executable query-plan evidence. | Query-count regression test, Postgres query-plan audit, and staging latency check on realistic seeded data. | in_progress |
| PERF-WATCH-001 | Watch context can regress by loading broad chapter/section graphs. | Add query-count regression coverage around section watch context. | Query-count test on realistic multi-chapter seed. | verified |
| PERF-XP-001 | XP award paths may be query-heavy when grading or updating progress. | Measure query count and batch writes where proven. | Query-count test before and after optimization. | verified |
| PERF-LIVE-001 | Live interaction writes must not grow query count per class size. | Add query-count coverage around student live interaction creation. | Query-count test proving bounded request-path SQL. | verified |
| PERF-PAGE-001 | Professor chat conversations/messages and topic comments can grow without bounds. | Add bounded default pagination while preserving existing list response envelopes. | Endpoint tests proving default-compatible limits, cursor/offset paging, ordering, and validation caps. | verified |

## P6 Frontend Resilience

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| FE-ERROR-001 | Core routes can collapse into blank or toast-only failure states. | Add route/widget error boundaries and explicit retry/forbidden/empty states. | Frontend tests and Playwright assertions for error states. | verified |
| FE-DATA-001 | Client data fetching is ad hoc and lacks consistent retry/cache/error policy. | Introduce SWR or React Query incrementally on core journeys. | Tests proving non-destructive auth/profile failure and retry behavior. | verified |
| FE-DEMO-001 | Demo strings and local-only video fallback can leak into production UX. | Gate or remove production-visible demo fallbacks. | Production-host tests proving no demo login/video/mock strings. | in_progress |

## P7 DevOps And Release

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| OPS-STAGE-001 | Staging is not a first-class release environment. | Add staging deploy skeleton with separate secrets, DB, S3 bucket, frontend/backend URLs, and release runbooks. | CI/deploy workflow evidence, runbook evidence, staging smoke result, rollback drill, and backup/restore drill. | in_progress |
| OPS-RDS-001 | RDS TLS verification and connection pooling strategy are not fully proven. | Set a CA trust store through `PGSSLROOTCERT`, require `sslmode=verify-full`, and require an RDS Proxy connection strategy for Lambda. | Deploy config, startup validation, diagnostics output, RDS Proxy target health, and staging DB connection evidence. | in_progress |
| OPS-LAMBDA-001 | Lambda runtime settings still reflect prototype defaults. | Increase memory/timeout, enable keep-warm, and record cold-start/runtime evidence before production. | Zappa config tests and staging Lambda duration/cold-start evidence. | in_progress |
| OPS-RUNBOOK-001 | Rollback, migration rollback, backup/restore, and incident response are not operationalized. | Add production runbooks and drill them on staging. | Runbook docs, tests preventing runbook drift, and completed staging drills. | in_progress |
| OPS-READY-001 | `/ready` only checks config and DB. | Keep `/ready` fast; add authenticated/internal production diagnostics for storage, realtime, video, email, and migration state. | Readiness tests and protected diagnostics tests. | verified |

## Evidence Log

| Date | Item | Evidence |
| --- | --- | --- |
| 2026-05-27 | OPS-FREEZE-001 executable production freeze | `python -m pytest tests_fastapi/test_launch_gate.py tests_fastapi/test_staging_runtime_verifier.py tests_fastapi/test_startup_security.py -q` passed with 27 tests. `python -m py_compile scripts/check_production_launch_gate.py scripts/check_staging_runtime.py` passed. Backend and frontend deploy workflows are now manual-only; production paths run `scripts/check_production_launch_gate.py`, which currently fails closed with 14 incomplete traceability rows and a 5.5/10 score below the 9/10 target. Backend deploys now run `scripts/check_staging_runtime.py` after `zappa schedule` to verify `/ready`, protected diagnostics, RDS Proxy declaration, S3 media config, migration heads, realtime outbox health, video/email config, and a bounded protected outbox-drain call. |
| 2026-05-27 | SEC-CSRF-001 implementation | `python -m pytest tests_fastapi/test_profile.py tests_fastapi/test_payments.py tests_fastapi/test_professor_platform.py tests_fastapi/test_csrf.py` passed. `python -m pytest tests_fastapi` passed with 198 tests. Route coverage now includes profile media upload, payment checkout initiation, professor live create/update/credential reveal, student live interaction, student chat, professor chat message, and professor chat image upload. |
| 2026-05-27 | SEC-CSP-001 implementation | `npm run test -- proxy.test.ts authSession.test.ts`, `npm run test`, `npm run typecheck`, `npm run lint`, and `npm run build` passed. Build output shows App Router routes are dynamic, enabling nonce-based CSP injection. |
| 2026-05-27 | SEC-LIVE-001 implementation | `python -m pytest tests_fastapi/test_professor_platform.py`, `python -m pytest tests_fastapi`, `npm run test`, `npm run typecheck`, `npm run lint`, `npm run build`, and `npm run test:e2e -- tests/e2e/next16-smoke.spec.ts` passed. Broad professor live DTOs expose `has_stream_credentials` only; `POST /api/professor/live-sessions/{id}/stream-credentials/reveal` returns credentials with `Cache-Control: no-store` and writes an audit row. |
| 2026-05-27 | SEC-ADMIN-001 implementation | `python -m pytest tests_fastapi/test_sqladmin_auth.py tests_fastapi/test_startup_security.py` passed with 16 tests. `python -m pytest tests_fastapi` passed with 202 tests. SQLAdmin now authenticates active, verified `users.is_staff` accounts with real password hashes, audits login/logout/action attribution, rechecks staff status and `auth_token_version` on each request, rejects hostile-origin admin POSTs, and deploy/startup config no longer requires `ADMIN_PASSWORD`. `rg -n "ADMIN_PASSWORD\|admin_password\|AdminAuth" backend docs` returns only `StaffAdminAuth` references and this traceability row. |
| 2026-05-27 | SEC-SECRETS-001 secret hygiene scanner | `python scripts/check_secret_hygiene.py` passed on tracked files. `python -m pytest tests_fastapi/test_secret_hygiene.py -q` passed with 5 tests. `python -m py_compile scripts/check_secret_hygiene.py` passed. CI and deploy workflows now run a redacting secret scanner that detects high-confidence provider tokens, private keys, and literal sensitive env values without printing the secret value. `docs/secrets-rotation-checklist.md` defines the provider-side rotation record required before release. `python scripts/check_secret_hygiene.py --include-local-env` currently fails on ignored `backend/.env` entries for `JWT_SECRET_KEY`, `GOOGLE_CLIENT_ID`, `VDOCIPHER_API_SECRET`, `STRIPE_SK`, `STRIPE_PRODUCT_ID`, `ADMIN_PASSWORD`, `RESEND_API_KEY`, and `ABLY_API_KEY`; values were not printed. This remains `in_progress` until those credentials are rotated/revoked or proven non-real and provider-side evidence is attached. |
| 2026-06-05 | SEC-SECRETS-001 local scan and checklist gate refresh | `python -m py_compile scripts/check_secret_hygiene.py` and `python scripts/check_secret_hygiene.py` passed. From `backend`, `python -m pytest tests_fastapi/test_secret_hygiene.py -q` passed with 15 tests, adding coverage that git-ignored env files are discovered without including tracked examples, findings redact literal values, token-only env names are sensitive while token TTL/version/file metadata is not, JWT/OIDC-shaped env values are detected, literal production-shaped `DATABASE_URL` values are not hidden by generic PostgreSQL placeholder text, `--include-local-env` output stays redacted, and the rotation checklist gate detects placeholders and missing required records. `docs/secrets-rotation-checklist.md` includes explicit staging and production `KRESCO_RATE_LIMIT_STORAGE_URI` inventory rows, with a regression proving the checked-in checklist inventories every required secret. `python scripts/check_secret_hygiene.py --include-local-env` now fails closed without printing secrets because `frontend/.env.inspect` contains a redacted JWT/OIDC token-shaped `VERCEL_OIDC_TOKEN` value. `python scripts/check_secret_hygiene.py --include-local-env --require-rotation-checklist` also fails closed because provider-evidence placeholders remain incomplete. `python scripts/check_production_launch_gate.py --json` still reports `SEC-SECRETS-001` as `in_progress`. This row must remain `in_progress` until local token material is removed/rotated, provider-side rotation/revocation evidence is attached, and the checklist gate passes. |
| 2026-06-05 | SEC-SECRETS-001 structured checklist enforcement | `scripts/check_secret_hygiene.py --require-rotation-checklist` now parses the rotation table as structured records instead of checking for identifier text only. It requires the exact staging/production/deploy record matrix, rejects blank or embedded `TBD`/`pending`/`N/A` placeholder language, requires parseable UTC timestamps, requires affirmative old-value revocation, rejects negative revocation phrasing such as `not revoked`, rejects weak evidence cells such as `done`, `link`, or unlinked digit notes, stops at the end of the rotation table so later markdown tables are ignored, and handles escaped pipes in markdown evidence links. Production backend and frontend deploy workflows run `python scripts/check_secret_hygiene.py --require-rotation-checklist` directly before the production launch gate. Local validation: from `backend/`, `python -m pytest tests_fastapi/test_secret_hygiene.py -q` passed with 22 tests and `python -m pytest tests_fastapi/test_secret_hygiene.py tests_fastapi/test_launch_gate.py -q` passed; `python scripts/check_secret_hygiene.py` passed; `python scripts/check_secret_hygiene.py --require-rotation-checklist` fails closed on the current placeholder evidence fields. `SEC-SECRETS-001` remains `in_progress` until the real checklist records are completed and the ignored-env token finding is removed/rotated. |
| 2026-05-27 | MEDIA-S3-001/MEDIA-AUTH-001 partial implementation | `python -m pytest tests_fastapi/test_professor_platform.py tests_fastapi/test_profile.py tests_fastapi/test_image_uploads.py tests_fastapi/test_startup_security.py` passed with 35 tests. `python -m pytest tests_fastapi` passed with 207 tests. `npm run typecheck` and `npm run test` passed. Upload routes now use a media storage abstraction; production-like startup requires `MEDIA_STORAGE_BACKEND=s3`, bucket, and region; `/media` is only mounted outside production-like environments; S3 uploads store `s3://bucket/key` references and return presigned read URLs with a cached boto3 client; profile saves avoid writing temporary presigned URLs back over stable storage references. This remains `in_progress` until staging Lambda upload verification, S3 lifecycle/quota rules, and unauthorized access checks are complete. |
| 2026-05-27 | RT-OUTBOX-001/RT-FANOUT-001 partial implementation | `python -m pytest tests_fastapi/test_realtime.py tests_fastapi/test_professor_platform.py tests_fastapi/test_startup_security.py` passed with 37 tests. `python -m pytest tests_fastapi/test_realtime.py` passed with 13 tests after exception-path hardening. `python -m pytest tests_fastapi/test_realtime.py tests_fastapi/test_professor_platform.py` passed with 27 tests after offering-channel broadcast work. `python -m pytest tests_fastapi` passed with 213 tests. `npm run test` passed with 112 tests and `npm run typecheck` passed. Added `realtime_outbox`, transactional enqueueing for professor live/chat events, retry/backoff/dead-letter processing, a protected `/api/internal/realtime/process-outbox` endpoint, `REALTIME_OUTBOX_SECRET` startup/deploy validation, single-client reuse while draining a batch, immediate retry/dead marking for unexpected publish exceptions, backend-authorized offering notification channels, and frontend live/calendar subscriptions to `/api/realtime/subscriptions`. Request-path per-student Ably HTTP calls are removed and a 50-student unit/integration test proves one offering event plus one session event, but this remains `in_progress` until scheduled EventBridge/SQS invocation and a real 50-student staging load test are complete. |
| 2026-05-27 | OPS-RDS-001 partial implementation | `python -m pytest tests_fastapi/test_database_url.py tests_fastapi/test_startup_security.py` passed with 18 tests. `python -m pytest tests_fastapi` passed with 215 tests. Bundled AWS RDS global CA at `backend/certs/rds-global-bundle.pem` from the AWS RDS trust store. Startup validation now requires production PostgreSQL URLs to use `sslmode=verify-full` and a readable CA trust store; asyncpg connections build a CA-backed `SSLContext` instead of passing libpq strings. RDS Proxy deployment uses `PGSSLROOTCERT=certifi` because RDS Proxy presents AWS Certificate Manager certificates chained to Amazon Trust Services, not the RDS instance CA. This remains `in_progress` until a staging RDS connection proves certificate verification against the actual endpoint. |
| 2026-05-27 | OPS-READY-001 implementation | `python -m pytest tests_fastapi/test_readiness.py` passed with 6 tests. `python -m pytest tests_fastapi/test_readiness.py tests_fastapi/test_startup_security.py tests_fastapi/test_realtime.py` passed with 34 tests. `/health` remains liveness-only, `/ready` remains cheap, and `GET /api/internal/diagnostics` now requires `x-kresco-internal-secret` and reports database reachability, Alembic migration heads, S3 media configuration, Ably key/outbox state, VdoCipher HTTPS configuration, and Resend configuration without returning secret values. Dead realtime outbox rows and migration drift make the diagnostics payload `not_ready`. |
| 2026-05-27 | PERF-TOPIC-001/PERF-WATCH-001/PERF-XP-001/PERF-LIVE-001 measurement | `python -m pytest tests_fastapi/test_query_counts.py` passed with 4 tests. `python -m pytest tests_fastapi/test_query_counts.py tests_fastapi/test_gating.py tests_fastapi/test_topic_quiz.py tests_fastapi/test_professor_platform.py` passed with 35 tests. `python -m pytest tests_fastapi` passed with 222 tests. Added a shared SQL query counter fixture and HTTP-level query-count regression tests for topic workspace search, section watch context, legacy quiz submit with XP awards, and student live interaction creation. Topic workspace and XP remain `in_progress` because the required targeted SQL/batched XP follow-up has not been implemented yet. |
| 2026-05-27 | PERF-TOPIC-001 targeted SQL implementation | `python -m pytest tests_fastapi/test_gating.py tests_fastapi/test_query_counts.py tests_fastapi/test_topic_search.py tests_fastapi/test_course_access_projection.py` passed with 28 tests. `python -m pytest tests_fastapi` passed with 222 tests. `get_topic_workspace` now loads topic metadata, sections, published items, item primary resources, published tabs, progress, and search notes through explicit targeted queries instead of hydrating the broad parent object graph through `Topic.sections` and `Topic.resources`. The workspace search query-count budget is tightened to `<= 8` SQL statements. This remains `in_progress` until a latency check on realistic seeded data and query-plan/index review are recorded. |
| 2026-05-27 | PERF-TOPIC-001 query-plan/index audit | `python -m pytest tests_fastapi/test_query_plan_audit.py tests_fastapi/test_query_counts.py tests_fastapi/test_readiness.py tests_fastapi/test_startup_security.py -q` passed with 36 tests. `python -m py_compile scripts/audit_query_plans.py alembic/versions/0026_realtime_outbox.py alembic/versions/0027_media_quota_counters.py alembic/versions/0028_hot_path_composite_indexes.py app/models/courses.py app/models/gamification.py app/models/interactions.py` passed. A temporary SQLite migration from base to head plus `python scripts/audit_query_plans.py` passed. Added composite hot-path indexes for topic sections, topic items, tab contents, topic item progress, user notes, chapters, and chapter sections, plus `backend/scripts/audit_query_plans.py` wired into backend CI/deploy against disposable Postgres. The audit checks index presence and `EXPLAIN` output for workspace/watch predicates. `0026_realtime_outbox` and `0027_media_quota_counters` were made idempotent after the migration rehearsal exposed baseline drift. This remains `in_progress` until the Postgres CI/deploy audit output and staging latency numbers on realistic course data are recorded. |
| 2026-06-05 | PERF-TOPIC-001 local plan/query/payload guard refresh | `python -m pytest tests_fastapi/test_query_plan_audit.py tests_fastapi/test_course_access.py -q` passed with 30 tests. `python -m py_compile scripts/audit_query_plans.py app/services/course_topic_read_models.py` passed. `python scripts/audit_query_plans.py` passed against the existing local SQLite database. The plan audit now separately checks the workspace progress lookup (`user_id`, `topic_id`, `topic_item_id`) against `ix_topic_item_progress_user_topic_item` and topic-card completed progress lookup against `ix_topic_item_progress_user_item_status`; course access tests cover inactive/search payload body compaction, capped search results, stable workspace query count, and stable workspace search query count. This remains `in_progress` because this local run does not prove staging latency on realistic seeded data or live staging/Postgres evidence. |
| 2026-05-27 | PERF-XP-001 bulk XP implementation | `python -m pytest tests_fastapi/test_query_counts.py tests_fastapi/test_xp_service.py tests_fastapi/test_topic_quiz.py tests_fastapi/test_gating.py` passed with 30 tests. `python -m pytest tests_fastapi` passed with 224 tests. Added `award_xp_bulk` with one logical `XPTransaction` per award, conflict-safe idempotency handling, aggregate `UserXP` updates, and aggregate daily quest progress updates. Legacy quiz submit now batches `quiz_pass`/`quiz_perfect`; tab quiz submit batches `QuestionAttempt` inserts and all `quiz_correct`/`quiz_pass` XP rows. Query-count gates now cover direct bulk XP, legacy quiz submit (`<= 20` SQL statements), and a 10-question tab quiz submit (`<= 24` SQL statements, down from 32 in the local regression run). |
| 2026-05-27 | PERF-PAGE-001 bounded list implementation | `python -m pytest tests_fastapi/test_professor_platform.py tests_fastapi/test_interactions.py -q` passed with 24 tests. `python -m pytest tests_fastapi/test_image_uploads.py tests_fastapi/test_profile.py -q` passed with 10 tests, confirming the cached S3 presign path still holds. `npm run typecheck` passed. `python -m pytest tests_fastapi -q` passed with 227 tests. Professor chat conversations now accept `limit/offset`, professor and student chat messages accept `limit/before_id` and return the newest page in chronological order, and topic comments accept `limit/offset` with validation caps. Frontend professor chat wrappers accept optional paging params without changing existing callers. |
| 2026-05-27 | OPS-STAGE-001 stage-aware deploy skeleton | `python -m pytest tests_fastapi/test_startup_security.py -q` passed with 16 tests. `backend/zappa_settings.json` now has separate `production` and `staging` stages, with `FRONTEND_URL` and `CORS_ALLOWED_ORIGINS` supplied through deploy-time placeholders instead of checked-in Vercel host literals. `scripts/render_zappa_settings.py` can render/validate a selected stage, and `.github/workflows/deploy-backend.yml` supports manual staging deploys through GitHub Environments while keeping runtime secret-backed values such as `REALTIME_OUTBOX_SECRET` and `MEDIA_S3_BUCKET` in AWS Secrets Manager. This remains `in_progress` until a real staging deploy, diagnostics check, rollback drill, backup/restore evidence, and production promotion runbook are complete. |
| 2026-05-27 | E2E-PROF-001 chat integration slice | `npm run typecheck` passed. `npm run test:e2e:integration -- --grep "backend-backed VIP student chat"` passed with 1 backend-backed browser test. `npm run test:e2e:integration` passed with 2 tests. Added a Playwright integration path that boots the real local backend and Next app, logs in through the public UI as seeded VIP student `vip@kresco.local`, opens `/professor-chat`, waits for seeded conversation data from `/api/professor/student-chat`, sends a chat message through the UI, waits for the real `POST /api/professor/student-chat/conversations/{id}/messages` 201 response, and asserts the rendered message. At that point this remained `in_progress` until professor live/session/checkpoint coverage, professor-side visibility, upload flow coverage, and error/forbidden/empty browser states were split or added. |
| 2026-05-27 | SEC-CSP-001 Next chunk compatibility | `npm run test -- proxy.test.ts authSession.test.ts`, `npm run typecheck`, and `npm run test:e2e:integration` passed. The backend-backed student E2E exposed that the first CSP implementation blanked `/home` because `strict-dynamic` disabled same-origin Next static chunks. The CSP now keeps nonce-gated inline scripts and no script `unsafe-inline`, while allowing same-origin Next chunks and Google auth styles. |
| 2026-05-27 | SEC-CSP-001 route coverage hardening | `npm run test -- proxy.test.ts` passed with 10 tests. The proxy tests now assert CSP, Referrer-Policy, and Permissions-Policy on representative landing, student, admin, professor, live, and login surfaces, and explicitly document that `/api`, `/media`, and static assets sit outside the frontend page CSP matcher. `style-src 'unsafe-inline'` remains tracked separately as `SEC-CSP-STYLE-001`. |
| 2026-05-27 | SEC-CSP-STYLE-001 CSP directive split and inventory | `npm run audit:csp-styles -- --json` passed and reported `broadStyleSrcAllowsInline=false`, `style-src 'self' https://accounts.google.com`, `style-src-elem 'self' 'unsafe-inline' https://accounts.google.com`, `style-src-attr 'unsafe-inline'`, 56 files with inline-style debt, 114 inline style attributes, 0 `style jsx` blocks, 0 `dangerouslySetInnerHTML`, 0 files with CSSOM style writes, and 0 CSSOM style writes after converting `frontend/app/auth/reset-password/page.tsx`, `frontend/app/auth/verify-email/page.tsx`, `frontend/app/page.tsx`, `frontend/components/Leaderboard.tsx`, `frontend/app/(dashboard)/calendar/page.tsx`, `frontend/app/figma-audit/page.tsx`, `frontend/app/professor/live/page.tsx`, `frontend/app/professor/live/[sessionId]/page.tsx`, `frontend/app/watch/[lessonId]/page.tsx`, `frontend/components/KrescoMascot.tsx`, `frontend/components/simulators/DiffractionSimulator.tsx`, `frontend/components/animated/source-ports/math/math-sets-lab/components/PascalTriangleAnimation.tsx`, `frontend/components/animated/source-ports/optics/light-lab/pages/DiffractionPage.tsx`, `frontend/components/figma/skeletons.tsx`, `frontend/components/figma/home.tsx`, `frontend/components/figma/subject-course-card.tsx`, `frontend/components/figma/progress.tsx`, and `frontend/components/figma/permanent-sidebar.tsx` away from render-time inline style attributes, inline style blocks, direct HTML injection, or DOM style mutation. `frontend/components/SafeRichText.tsx` now sanitizes watch text and renders a structured React tree instead of using `dangerouslySetInnerHTML`; the calendar route now renders event geometry through SVG/HTML attributes and tokenized background classes instead of mutating button/detail-card style objects; diffraction drag cursors use a cleaned-up CSS class instead of `document.body.style`; Pascal canvas sizing keeps backing-store dimensions while relying on existing CSS classes for layout. `npm run test -- safeRichText.test.ts watchViewModel.test.ts proxy.test.ts` passed with 18 tests. The audit now also enforces those reduced counts plus zero CSSOM write budgets as regression gates, so CI fails if future changes increase the remaining debt or hide inline-style debt behind DOM style mutation. `npm run test -- proxy.test.ts` passed with 10 tests. `npm run test` passed with 166 tests. `npm run typecheck`, `npm run lint`, and `npm run build` passed. `npm run test:e2e -- tests/e2e/next16-smoke.spec.ts --grep "public auth and reset-password routes hydrate|authenticated dashboard, payment, and admin routes hydrate"` passed with 2 browser smoke tests. Frontend CI now runs `npm run audit:csp-styles`. The broad `style-src 'unsafe-inline'` fallback is removed and inline style allowance is isolated to CSP3 element/attribute directives as a transitional hardening step. This remains `in_progress` because the scanner proves the app still depends on inline style attributes; next work must retire generated animated simulator surfaces and remaining core outliers, then remove `style-src-attr 'unsafe-inline'`, verify whether Google auth or other runtime paths still require `style-src-elem 'unsafe-inline'`, and remove it if production browser evidence permits. |
| 2026-06-05 | SEC-CSP-STYLE-001 inline-style migration verified | `npm run audit:csp-styles -- --json` passed with zero budgets and reported `filesWithInlineStyleDebt=0`, `inlineStyleAttributes=0`, `styleJsxBlocks=0`, `dangerouslySetInnerHTML=0`, `cssomStyleWrites=0`, `dynamicStyleTagCreations=0`, `dynamicStyleContentWrites=0`, `styleSrcElemAllowsInline=false`, and `styleSrcAttrAllowsInline=false`. `style-src-attr` is now `none`; `style-src-elem` allows only self, Google auth styles, and the two runtime style hashes observed during production browser smoke. `npm run test -- proxy.test.ts nextConfig.test.ts pdfViewerStorage.test.ts zedModeOverlay.test.ts`, `npm run typecheck -- --pretty false`, scoped ESLint, `npm run test`, `npm run lint`, and `npm run build` passed. `npm run test:e2e -- tests/e2e/next16-smoke.spec.ts` passed with 6 Chromium smoke tests after the browser console collector was hardened to fail on CSP violations; the smoke uses an allowed mocked VdoCipher frame URL rather than a `data:` iframe. |
| 2026-05-27 | E2E-STUDENT-001 student integration verified | `python scripts/prepare_e2e_db.py` passed and the seeded SQLite fixture contains `E2E Watch Flow` text sections. `npm run test -- watchViewModel.test.ts` passed with 5 tests. `npm run test` passed with 114 tests. `npm run typecheck` passed. `npm run test:e2e:integration -- --grep "backend-backed student journey"` passed with 1 browser test. `npm run test:e2e:integration` passed with 3 tests. `python -m pytest tests_fastapi/test_query_counts.py tests_fastapi/test_gating.py -q` passed with 19 tests. `python -m pytest tests_fastapi -q` passed with 229 tests. The backend-backed Playwright path now logs in through the real UI as `student@kresco.local`, loads `/home` from real course APIs, opens a real `/topics/{id}` workspace, posts `POST /api/courses/topic-items/{id}/complete`, loads `/watch/{sectionId}` through real watch/access APIs, posts `POST /api/progress/section-complete`, and verifies the completed watch state. |
| 2026-05-27 | E2E-PROF-001 live integration verified | `npm run typecheck` passed. `python -m pytest tests_fastapi/test_professor_platform.py tests_fastapi/test_realtime.py -q` passed with 29 tests. `npm run test:e2e:integration -- --grep "backend-backed professor live session"` passed with 1 browser test. `npm run test:e2e:integration` passed with 4 tests. `npm run test` passed with 114 tests. `python -m pytest tests_fastapi -q` passed with 229 tests. The backend-backed Playwright path logs in through the real UI as `professor@kresco.local`, creates a live session through `/professor/live`, starts it through the professor action menu, creates a checkpoint through the authenticated backend API with CSRF headers, logs in as seeded VIP student `vip@kresco.local`, verifies the new session is joinable in `/live`, loads the real student embed endpoint, verifies the checkpoint via the student checkpoint API, sends a Q&A interaction through the student room, opens the professor control room, and marks the student question answered through the real professor UI. The run also exposed and fixed the local full seed gap where professor-demo VIP/platinum users lacked Math subject entitlements after entitlement reset. Upload and negative-state browser gates remain tracked separately as `E2E-UPLOAD-001` and `E2E-NEGATIVE-001`. |
| 2026-05-27 | E2E-UPLOAD-001 S3-mock browser upload verified | `python -m pytest tests_fastapi/test_image_uploads.py tests_fastapi/test_profile.py tests_fastapi/test_professor_platform.py -q` passed with 26 tests. `python -m pytest tests_fastapi/test_image_uploads.py tests_fastapi/test_startup_security.py tests_fastapi/test_readiness.py -q` passed with 27 tests. `npm run typecheck` passed. `npm run test:e2e:integration -- --grep "backend-backed upload flows"` passed with 1 browser test. `npm run test:e2e:integration` passed with 5 tests. `npm run test` passed with 114 tests. `python -m pytest tests_fastapi -q` passed with 230 tests. Added a non-production `MEDIA_STORAGE_BACKEND=s3-mock` storage backend and Playwright integration env that stores upload objects under an S3-shaped bucket/prefix, persists stable `s3://kresco-e2e-media/e2e/...` references, and returns mock presigned URLs rather than public `/media` paths. The browser test logs in through the real UI, uploads profile avatar and banner from the profile editor, verifies `/api/profile/me` returns the presigned mock URLs, uploads a student professor-chat image through `/professor-chat`, and uploads a professor reply image through `/professor/chat`. This verifies the backend-backed S3-mock upload gate; staging Lambda upload, lifecycle/quota, and real bucket policy evidence remain under `MEDIA-S3-001`/`MEDIA-AUTH-001`. |
| 2026-05-27 | E2E-NEGATIVE-001 negative browser states verified | `npm run test -- watchViewModel.test.ts authGuardComponent.test.ts authSession.test.ts proxy.test.ts` passed with 34 tests. `npm run typecheck` passed. `npm run test:e2e:integration -- --grep "backend-backed negative states"` passed with 1 browser test. `npm run test:e2e:integration` passed with 6 tests. `npm run test` passed with 115 tests. `npm run build` passed. `python -m pytest tests_fastapi/test_course_access_projection.py::test_exam_projection_redacts_locked_problem_and_video_resource tests_fastapi/test_gating.py::test_exam_bank_reports_access_policy_and_searches_exam_metadata -q` passed with 2 tests. `python -m pytest tests_fastapi/test_readiness.py tests_fastapi/test_startup_security.py tests_fastapi/test_notifications.py tests_fastapi/test_query_counts.py -q` passed with 29 tests. `python -m pytest tests_fastapi -q` passed with 230 tests. Added backend-backed Playwright coverage for expired auth cookie redirect plus cookie clearing, student forbidden access to `/admin` with a non-blank `Staff access required` surface, controlled `/api/admin/overview` 500 fallback UI, and controlled empty `/api/courses/topics` plus `/api/courses/subjects` home empty states. AuthGuard now separates unauthorized logout from forbidden and retryable profile-verification failures, keeping the session intact on backend 500 with a retry UI. The run also fixed the watch text fixture contract (`text_content || content`), removed a duplicate notification index declaration, made E2E SQLite prep recreate the file cleanly, and restored exam-bank locked problem redaction to preserve statements while hiding private solutions/media secrets. This verifies the negative browser-state gate only; broader route/widget error boundaries and SWR/React Query rollout remain under `FE-ERROR-001`/`FE-DATA-001`. |
| 2026-05-27 | MEDIA-S3-001/MEDIA-AUTH-001 local quota and lifecycle guardrails | `python -m pytest tests_fastapi/test_profile.py tests_fastapi/test_professor_platform.py tests_fastapi/test_image_uploads.py tests_fastapi/test_startup_security.py tests_fastapi/test_readiness.py -q` passed with 51 tests. `python -m pytest tests_fastapi/test_image_uploads.py tests_fastapi/test_profile.py tests_fastapi/test_professor_platform.py -q` passed with 30 tests. `python -m pytest tests_fastapi -q` passed with 236 tests. Added persistent profile avatar/banner byte counters, aggregate per-profile quota enforcement, aggregate per-conversation professor-chat image quota enforcement, production settings validation for media quotas and lifecycle-retention days, deploy environment variables for those controls, diagnostics reporting for quota/lifecycle media settings, and tests proving S3 writes use private server-side encrypted objects without public ACLs. `docs/manual-operations.md` now documents the required Block Public Access posture, anonymous-read denial check, and a concrete S3 lifecycle configuration for staging/production prefixes. These rows remain `in_progress`: local tests and documentation do not prove staging Lambda upload success, real private bucket policy enforcement, or installed lifecycle rules in AWS. |
| 2026-06-05 | MEDIA-S3-001/MEDIA-AUTH-001 scoped presign hardening | `python -m pytest tests_fastapi/test_image_uploads.py -q` passed with 19 tests. `python -m pytest tests_fastapi/test_professor_platform.py::test_vip_student_can_start_one_conversation_and_professor_can_reply tests_fastapi/test_professor_platform.py::test_subject_scoped_vip_student_chat_rejects_same_track_locked_subject tests_fastapi/test_professor_platform.py::test_professor_chat_image_upload_enforces_conversation_quota tests_fastapi/test_professor_platform.py::test_professor_chat_image_upload_uses_configured_storage tests_fastapi/test_profile.py::test_upload_profile_avatar_uses_configured_s3_storage tests_fastapi/test_profile.py::test_profile_media_upload_enforces_aggregate_quota -q` passed with 6 tests. Media URL serialization now refuses to presign `s3://` references outside the configured media bucket/prefix for both `s3` and `s3-mock`; wrong-bucket, wrong-prefix, and missing configured-bucket references serialize as empty URLs instead of temporary read URLs. This adds local object-key scoped URL evidence only. These rows remain `in_progress` until staging Lambda upload, real private bucket policy anonymous-read denial, and installed AWS lifecycle rules are verified. |
| 2026-06-05 | MEDIA-S3-001/MEDIA-AUTH-001 S3 posture evidence collector | `python -m py_compile scripts/check_s3_media_posture.py` passed. From `backend/`, `python -m pytest tests_fastapi/test_s3_media_posture_verifier.py -q` passed with 8 tests. Added `scripts/check_s3_media_posture.py`, a redacting local collector that uses `boto3` or the AWS CLI to check bucket-level Block Public Access, default bucket encryption, lifecycle expiration coverage for a media prefix, and anonymous ranged-GET denial against an existing object using virtual-hosted-style S3 object URLs. This is an evidence collector only; it was tested with mocked AWS calls and has not been run against real staging AWS in this repository session. `MEDIA-S3-001` and `MEDIA-AUTH-001` remain `in_progress` until the collector output is captured from the actual staging bucket/prefix alongside staging Lambda upload and authenticated presigned-read evidence. |
| 2026-06-05 | MEDIA/RT/OPS/PERF staging launch evidence workflow | Added `.github/workflows/staging-launch-evidence.yml`, a manual staging Environment workflow that configures AWS credentials, runs the structured rotation checklist gate, collects staging runtime diagnostics, S3 media posture, ops posture, realtime outbox, realtime fanout-50 provider delivery, and topic latency evidence, then uploads redacted JSON artifacts. Collector steps run through `scripts/run_evidence_command.py`; JSON collectors now require a single valid JSON stdout document, command failures still write redacted wrapper artifacts when possible, staging secrets are scoped to only the collector step that needs them, and a pre-upload validation step checks every expected artifact is present, nonempty, and parseable JSON. Backend deploy also runs `scripts/check_s3_media_posture.py` after post-deploy runtime verification, so staging deploys can generate S3 posture evidence in the actual deploy path. Backend CI path filters now include workflow files inspected by backend tests. From `backend/`, `python -m pytest tests_fastapi/test_staging_launch_evidence_workflow.py tests_fastapi/test_run_evidence_command.py -q` passed with 7 tests, and the focused backend launch-evidence suite passed with 80 tests. This is local workflow enforcement only: the MEDIA, RT, OPS, and PERF rows remain `in_progress` until this workflow or deploy path runs successfully against real staging with artifacts attached. |
| 2026-05-27 | RT-OUTBOX-001 scheduled worker plumbing | `python -m pytest tests_fastapi/test_realtime.py tests_fastapi/test_startup_security.py -q` passed with 33 tests. `python -m py_compile app/scheduled.py` passed. `python -m json.tool zappa_settings.json` passed. `python -m pytest tests_fastapi --ignore=tests_fastapi/test_seed_local_full.py --ignore=tests_fastapi/test_seed_safety.py -q` passed with 227 tests; full `python -m pytest tests_fastapi -q` is blocked by tracked deletions of `backend/seed_burner_data.py`, `backend/seed_kresco_v1.py`, `backend/seed_local_full.py`, and `backend/seed_safety.py`, which make the two seed tests fail import collection before this outbox code runs. Added `app.scheduled.process_realtime_outbox_event`, production/staging Zappa `events` entries at `rate(1 minute)`, deploy workflow `zappa schedule "$ZAPPA_STAGE"` application, and tests proving the scheduled entrypoint drains the existing DB-backed outbox service with bounded limit parsing. This remains `in_progress` until a real staging deploy proves EventBridge is firing and a 50-student staging load test confirms the end-to-end fanout path. |
| 2026-06-05 | RT-FANOUT-001/RT-OUTBOX-001 local hardening | From `backend/`, `python -m pytest tests_fastapi/test_realtime_outbox.py tests_fastapi/test_realtime.py::test_internal_process_outbox_requires_worker_secret tests_fastapi/test_realtime.py::test_internal_process_outbox_fails_closed_when_worker_secret_is_unconfigured -q` passed with 8 tests; `python -m pytest tests_fastapi/test_realtime.py tests_fastapi/test_realtime_outbox.py tests_fastapi/test_realtime_fast_lane.py -q` passed with 34 tests; `python -m pytest tests_fastapi/test_professor_platform.py::test_live_session_notifications_use_single_offering_realtime_event tests_fastapi/test_professor_platform.py::test_professor_live_session_notify_is_idempotent_per_student -q` passed with 2 tests; `python -m pytest tests_fastapi/test_staging_runtime_verifier.py -q` passed with 8 tests; `python -m py_compile app/scheduled.py ..\scripts\check_staging_runtime.py` passed; and `python -m pytest tests_fastapi/test_startup_security.py::test_zappa_environments_match_startup_validation tests_fastapi/test_startup_security.py::test_backend_deploy_workflow_passes_required_stage_render_inputs -q` passed with 2 tests. Added regressions proving the outbox publisher never exceeds `OUTBOX_MAX_CONCURRENCY`, fresh worker-owned `publishing` rows are not duplicated while stale locks are reclaimed, and the protected outbox drain fails closed when `REALTIME_OUTBOX_SECRET` is shorter than the required internal-worker secret length. This remains `in_progress`: these are local code/tests only and do not verify staging EventBridge/SQS invocation, Ably provider delivery, or the required real 50-student staging fanout test. |
| 2026-06-05 | RT-FANOUT-001/RT-OUTBOX-001 staging evidence collector | `python -m py_compile scripts/check_staging_realtime_fanout.py` passed. From `backend/`, `python -m pytest tests_fastapi/test_staging_realtime_fanout_probe.py -q` passed with 8 tests. Added `scripts/check_staging_realtime_fanout.py`, which accepts the staging backend URL, internal worker secret, optional professor/student auth tokens, and optional Ably API key without printing secret values; `--mode outbox` collects bounded protected outbox-drain evidence, `--mode fanout-50` can trigger an existing staging live-session action and verify 50 eligible student subscription/token/session contracts, and `--require-provider-delivery` checks Ably history for the expected offering-channel event with structured redacted failures when provider history cannot be collected. Provider-delivery evidence now records the probe start timestamp and rejects matching Ably history items older than the professor action, so stale offering-channel history cannot satisfy a fresh fanout run. `fanout-50` fails closed when `expected_students` is below 50. This is an evidence collector only. These RT rows remain `in_progress` until a real staging run proves EventBridge scheduled invocation, provider delivery, and the required 50-student fanout. |
| 2026-05-27 | FE-DEMO-001 local demo media hardening | `npm run test -- devFeatures.test.ts productionEnv.test.ts nextConfig.test.ts figmaWorkspace.test.ts` passed with 18 tests. `npm run test` passed with 123 tests. `npm run typecheck`, `npm run lint`, and `npm run validate:production-env` passed. `rg -n "Local demo login\|Lecteur video de demo\|vrai ID video\|demo-login\|dQw4w9WgXcQ" frontend/app frontend/components frontend/lib -S` returned no matches. Local mock VdoCipher streams now require a non-production build, production env validation rejects local demo media flags, Next production config no longer whitelists localhost image optimization targets or local API rewrites, and generic Figma workspace video frames no longer default to the hardcoded YouTube placeholder. This remains `in_progress` until a production-host/browser run proves no demo login/video/mock strings can appear on the deployed host; local `next build` could not be rerun in this session because another Next process was already using `.next`. |
| 2026-06-05 | FE-DEMO-001 local rewrite boundary evidence | `npm run test -- nextConfig.test.ts productionEnv.test.ts devFeatures.test.ts` passed with 24 tests. `npm run typecheck -- --pretty false` passed. `npm run validate:production-env` passed when run with explicit production-shaped placeholders for `NEXT_PUBLIC_API_BASE_URL=/api/`, `KRESCO_BACKEND_ORIGIN=https://api.kresco.example`, `NEXT_PUBLIC_GOOGLE_CLIENT_ID`, `NEXT_PUBLIC_ABLY_ENABLED=true`, and a concrete `NEXT_PUBLIC_RELEASE_SHA`; the same command without those local placeholder values failed closed on missing `NEXT_PUBLIC_RELEASE_SHA` and `KRESCO_BACKEND_ORIGIN`. `next.config.mjs` now computes rewrites at `rewrites()` call time, and `nextConfig.test.ts` asserts that production-marked builds emit no localhost `/api` or `/media` rewrites even when local flags are present, development-marked integration builds can still target the local backend, and explicit HTTPS backend rewrites take precedence over local origins. This remains `in_progress` because this is local code/config evidence only, not deployed production-host browser proof that no demo login/video/mock strings appear. |
| 2026-06-05 | FE-DEMO-001 production-host scanner added | `npm run test -- productionDemoSurface.test.ts` passed with 6 tests. `npx eslint scripts/check-production-demo-surface.mjs tests/productionDemoSurface.test.ts --max-warnings=0` passed. `node scripts/check-production-demo-surface.mjs --json` failed closed without network access because no explicit production base URL was provided. Added `npm run check:production-demo-surface`, which requires `--base-url` or an explicit production base URL env var, performs credentialless GET checks against public/auth/dashboard-ish routes, scans same-origin referenced text assets, reports only sanitized source URLs/rule IDs, and fails on known demo login/video/mock placeholders plus localhost API/media origins, including French demo-video fallback text matched through ASCII-safe Unicode escapes. This is executable local scanner evidence only; `FE-DEMO-001` remains `in_progress` until this scanner is run against the deployed production host and that host evidence is recorded. |
| 2026-06-05 | FE-DEMO-001 production alias scan | Read-only Vercel inspection found project `mvp-v2` has a READY production deployment created 2026-06-02 with aliases including `kresco.ma`, `www.kresco.ma`, `mvp-v2-theta.vercel.app`, and `mvp-v2-kresco-s-projects.vercel.app`. From this environment, `kresco.ma` and `www.kresco.ma` did not resolve, while `mvp-v2-kresco-s-projects.vercel.app` returned Vercel SSO `401`; the usable public production alias was `https://mvp-v2-theta.vercel.app`. `npm run check:production-demo-surface -- --base-url https://mvp-v2-theta.vercel.app --json` exited `1`, fetched 13 routes and 25 same-origin text assets, found 0 demo/mock/local findings, and reported 1 error because `/onboarding` returned HTTP 404. This is real production-host evidence but not passing evidence: the scanner result is `ok: false`, and the deployed production build does not match the current workspace route set. `FE-DEMO-001` remains `in_progress` until a current production deployment passes the scanner on all required routes. |
| 2026-06-05 | FE-DEMO-001 production deploy scanner enforcement | `.github/workflows/deploy-frontend.yml` now runs a production-only post-deploy surface scan after capturing the Vercel deployment URL, normalizing it to HTTPS when needed, requiring `FRONTEND_PRODUCTION_BASE_URLS`, and executing `npm run check:production-demo-surface -- --base-url "$base_url" --json` for the fresh deployment URL plus every configured public production alias. From `backend/`, `python -m pytest tests_fastapi/test_launch_gate.py -q` passed with 6 tests, including workflow regressions that prevent removing the post-deploy scanner or alias-scan requirement. This is deploy-path enforcement only; `FE-DEMO-001` remains `in_progress` until a real current production deployment passes the scanner on the public aliases. |
| 2026-05-27 | OPS-RDS-001/OPS-LAMBDA-001/OPS-RUNBOOK-001 local deploy guardrails | `python -m pytest tests_fastapi/test_startup_security.py tests_fastapi/test_readiness.py -q` passed with 25 tests. `python -m pytest tests_fastapi/test_vdocipher_service.py::test_live_embed_urls_escape_ids_and_tokens tests_fastapi/test_startup_security.py tests_fastapi/test_readiness.py -q` passed with 26 tests. `python -m py_compile app/config.py app/services/diagnostics.py app/services/vdocipher.py scripts/render_zappa_settings.py` passed. `python -m json.tool zappa_settings.json` passed. `python -m pytest tests_fastapi --ignore=tests_fastapi/test_seed_local_full.py --ignore=tests_fastapi/test_seed_safety.py --ignore=tests_fastapi/test_data_integrity_audit.py --ignore=tests_fastapi/test_notifications.py -q` passed with 226 tests; the unignored full suite is still blocked by tracked deletions of seed, data-integrity, and notification service files outside this slice. Added `DATABASE_CONNECTION_STRATEGY=rds_proxy` startup/deploy validation, Zappa stage settings with `memory_size=1024`, `timeout_seconds=45`, `keep_warm=true`, diagnostics reporting of declared RDS Proxy strategy, and `docs/production-runbook.md` covering release preflight, deploy, rollback, migration rollback, backup/restore, and incident response. A missing `get_live_chat_embed_url` helper was restored after the broad backend run exposed the regression. These ops rows remain `in_progress` until staging proves the real RDS Proxy endpoint, Lambda cold-start/runtime profile, rollback drill, and backup/restore drill. |
| 2026-06-05 | OPS-STAGE-001/OPS-RDS-001/OPS-LAMBDA-001/OPS-RUNBOOK-001 non-Stripe staging verifier alignment | Recent GitHub staging diagnostics reached the deployed backend URL and reported email, video, realtime, and outbox checks as healthy while Stripe payment provider reachability returned `authentication_error`; no secret values were recorded. `scripts/check_staging_runtime.py` now treats payment as deferred for the current non-Stripe launch gate, reports a redacted `deferred_payment_check`, only fails payment configuration or reachability when `--require-payment-provider-reachability` is explicitly set, and still fails closed when `diagnostics.status` is `not_ready` without named deferred errors. `.github/workflows/staging-provider-diagnostics.yml` now reuses the runtime verifier with `--include-provider-reachability --json` instead of a hand-rolled Stripe failure path. `.github/workflows/deploy-backend.yml` preflights target `DATABASE_URL` shape before migrations, and `backend/scripts/render_zappa_settings.py` rejects Lambda runtime drift such as low memory, missing keep-warm, or a missing realtime outbox schedule. Local validation: `python -m py_compile scripts/check_staging_runtime.py backend/scripts/render_zappa_settings.py`; `cd backend; python -m pytest tests_fastapi/test_staging_runtime_verifier.py -q`; `cd backend; python -m pytest tests_fastapi/test_startup_security.py::test_render_zappa_settings_substitutes_placeholders_and_validates tests_fastapi/test_startup_security.py::test_render_zappa_settings_supports_staging_stage tests_fastapi/test_startup_security.py::test_zappa_environments_match_startup_validation -q`; `python scripts/check_production_launch_gate.py --json` failed closed with 11 incomplete rows and score `5.5/10` below target `9/10`. These OPS rows remain `in_progress`: local drift tests and a reached staging diagnostics URL do not prove RDS Proxy target health, TLS handshake, Lambda cold-start/runtime metrics, rollback drill, or backup/restore drill. |
| 2026-06-05 | OPS-STAGE-001 runtime JSON artifact hardening | `scripts/check_staging_runtime.py --json` now keeps readiness retry logs on stderr instead of stdout, so successful JSON output stays parseable for evidence artifacts. Runtime HTTP error payloads are redacted recursively for sensitive keys and token-shaped strings before being rendered into errors. From `backend/`, `python -m pytest tests_fastapi/test_staging_runtime_verifier.py -q` passed, including regressions for stderr retry logging and redacted error payloads. This is local verifier hygiene only; real staging runtime evidence is still required. |
| 2026-06-05 | OPS/PERF staging evidence collectors | `python -m py_compile scripts/check_staging_ops_posture.py scripts/check_staging_topic_latency.py` passed. From `backend/`, `python -m pytest tests_fastapi/test_staging_ops_posture.py tests_fastapi/test_staging_topic_latency.py -q` passed with 11 tests using mocked AWS and HTTP only. Added `scripts/check_staging_ops_posture.py` to collect redacted fail-closed staging evidence for RDS Proxy `RequireTLS`, RDS Proxy target health, Lambda memory/timeout, Lambda keep-warm schedule, realtime outbox worker schedule, and operator-supplied rollback/migration rollback/backup restore/incident drill JSON. Added `scripts/check_staging_topic_latency.py` to measure real staging topic workspace and workspace-search latency with a redacted auth header, HTTPS/non-local staging URL preflight, redacted non-JSON error bodies, and fail-closed contract mode when staging inputs are missing. These are evidence collectors only: no real AWS output, staging HTTP latency output, or drill artifacts are attached by this change, so `OPS-RDS-001`, `OPS-LAMBDA-001`, `OPS-RUNBOOK-001`, and `PERF-TOPIC-001` remain `in_progress`. |
| 2026-06-05 | Launch evidence audit from GitHub/AWS/staging | Read-only GitHub Actions inspection found remote `master` evidence is from 2026-06-02 and is partial for the local dirty workspace. Latest staging diagnostics, NAT egress, and realtime recovery workflows passed, but the latest backend deploy failed post-deploy runtime verification after an earlier successful deploy. `RT-OUTBOX-001` has strong partial recovery/drain evidence from runs `26825475805`, `26825504021`, and `26798565480`; `RT-FANOUT-001` still lacks a required fanout-50 provider run; `FE-DEMO-001` has no passing frontend deploy/scanner evidence; `OPS-RUNBOOK-001` has no completed drill evidence. Local AWS/staging collection is unavailable from this machine: `aws sts get-caller-identity` exits `255`, required AWS/staging env names are absent, no matching local artifacts exist under `artifacts/`, and the MEDIA/OPS/PERF/RT collectors all fail closed on missing credentials or required staging inputs. `python scripts/check_production_launch_gate.py --json` remains failed closed at `5.5/10` with 11 incomplete rows. |
| 2026-05-27 | FE-ERROR-001 route and widget fallback hardening | `npm run test -- errorBoundary.test.ts proxy.test.ts authGuardComponent.test.ts` passed with 19 tests. `npm run test` passed with 127 tests. `npm run typecheck`, `npm run lint`, and `npm run build` passed. `npm run test:e2e:integration -- --grep "backend-backed route fallbacks"` passed with 1 backend-backed browser test. `npm run test:e2e:integration` passed with 7 backend-backed browser tests. Added reusable route/widget fallback components, dashboard/admin/professor widget boundaries, leaf route error files for subject, topic, exam, watch, and professor live control surfaces, and explicit retryable nonblank fallbacks for subject detail, topic workspace, watch lesson, and exam load failures. The integration suite now deliberately forces subject, topic workspace, watch-context, and exam load failures and asserts nonblank retry/home UI while preserving the existing expired-token, forbidden, backend-500, empty-state, student journey, professor live, chat, and S3-mock upload gates. |
| 2026-05-27 | FE-DATA-001 core SWR data policy verified | `npm run test -- homePage.test.ts profilePage.test.ts apiData.test.ts` passed with 15 tests. `npm run test -- apiData.test.ts homePage.test.ts profilePage.test.ts profileViewModel.test.ts authGuardComponent.test.ts` passed with 26 tests. `npm run test -- topicWorkspaceData.test.ts topicWorkspaceViewModel.test.ts` passed with 10 tests. `npm run test -- watchData.test.ts watchViewModel.test.ts` passed with 8 tests. `npm run test -- examData.test.ts apiData.test.ts` passed with 6 tests. `npm run test -- professorDashboardData.test.ts apiData.test.ts` passed with 6 tests. `npm run test -- professorDashboardData.test.ts authGuardComponent.test.ts apiData.test.ts` passed with 13 tests. `npm run test -- professorChatData.test.ts apiData.test.ts` passed with 7 tests. `npm run test -- liveSessionData.test.ts professorChatData.test.ts apiData.test.ts` passed with 10 tests. `npm run test -- studentProfessorChatData.test.ts professorChatData.test.ts liveSessionData.test.ts apiData.test.ts` passed with 13 tests. `npm run test` passed with 163 tests. `npm run typecheck`, `npm run lint`, and `npm run build` passed. `npm run test:e2e:integration -- --grep "backend-backed upload flows\|backend-backed negative states"` passed with 2 backend-backed browser tests after rerunning serially; the first parallel attempt was blocked by Next's build lock, not by an app failure. `npm run test:e2e:integration -- --grep "backend-backed student journey\|backend-backed route fallbacks"` passed with 2 backend-backed browser tests after the topic and watch migrations. `npm run test:e2e:integration -- --grep "backend-backed route fallbacks"` passed with 1 backend-backed browser test after the exam migration. `npm run test:e2e -- tests/e2e/next16-smoke.spec.ts --grep "professor dashboard"` passed with 1 browser smoke test after the professor chat migration and later `npm run test:e2e -- tests/e2e/next16-smoke.spec.ts --grep "professor dashboard\|student live schedule"` passed with 2 browser smoke tests after live migration. `npm run test:e2e:integration -- --grep "backend-backed professor live session"` passed with 1 backend-backed browser test. `npm run test:e2e:integration -- --grep "backend-backed VIP student chat\|backend-backed upload flows"` passed with 2 backend-backed browser tests. `npm run test:e2e -- tests/e2e/next16-smoke.spec.ts --grep "vip student professor chat"` passed with 1 browser smoke test after a serial rerun; the first parallel attempt raced `next build` and failed before the server started because `.next` was being rebuilt. Added `swr`, a shared API SWR fetcher/retry policy, root `ApiDataProvider`, dashboard-home SWR loading/cache/retry behavior for `/courses/topics` and `/courses/subjects`, profile SWR loading/cache/retry behavior for `/profile/me`, XP/stats, notes/saves, subjects/topics, and sidebar summary, topic workspace SWR loading/cache/retry behavior for `/courses/topics/{id}/workspace` with URL-driven item selection preserved, watch SWR loading/cache/retry behavior for `/courses/sections/{id}/watch-context`, non-fatal `/progress/sections/{id}/access`, and video-only `/courses/lessons/{id}/pdfs`, exam SWR loading/cache/retry behavior that preserves ordered subject lesson quiz probing while skipping missing per-lesson quizzes, professor dashboard SWR loading/cache/retry behavior for `/professor/dashboard` with notify/start mutations revalidating the dashboard cache, professor chat SWR loading/cache/retry behavior for `/professor/chat/conversations` plus active `/professor/chat/conversations/{id}/messages`, student professor-chat SWR loading/cache/retry behavior for `/professor/student-chat` plus active `/professor/student-chat/conversations/{id}/messages`, and live-session SWR loading/cache/retry behavior for professor schedule, professor control room, student live schedule, and student live room. The migrated core surfaces keep cached or partial data visible on transient failures where safe, expose retry actions without clearing auth state, re-report repeated failures after recovery where user-visible toasts are used, prevent stale watch/exam/chat/live data from leaking across route ids, and keep profile save/upload, topic/watch completion, professor notify/start/control, student/professor chat send/edit/delete/pin/upload, and live Q&A refresh paths covered. |
