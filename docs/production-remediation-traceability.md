# Production Remediation Traceability

This file is the executable source for `scripts/check_production_launch_gate.py`. Rows stay `in_progress` until the listed evidence is captured from staging or dark production.

## Launch Gate

| ID | Finding | Required Change | Evidence Required | Status |
| --- | --- | --- | --- | --- |
| OPS-FREEZE-001 | Production deploys must fail closed until launch evidence is complete. | Keep deploy workflows manual-only and gated by the production launch checker. | CI/deploy logs showing the launch gate blocks production cutover below 9/10. | verified |
| SEC-CSRF-001 | Browser mutations require a consistent CSRF boundary. | Keep cookie auth and CSRF checks enabled for production-like environments. | Backend tests and browser smoke evidence for authenticated POST/PATCH flows. | verified |
| SEC-CSP-001 | Frontend security headers must allow only required origins. | Keep CSP generated from current Firebase, API, and media origins. | Frontend tests for CSP directives and deployed header capture. | verified |
| SEC-CSP-STYLE-001 | Inline style policy must stay bounded. | Keep style allowances explicit and audited. | Frontend CSP tests and deployed header capture. | verified |
| SEC-ADMIN-001 | Admin access must not rely on local/demo shortcuts. | Require real admin credentials and production-only auth checks. | Backend admin tests and production-shaped env validation. | verified |
| SEC-LIVE-001 | Live/video provider credentials must remain backend-only. | Keep VdoCipher and live creation secrets in runtime secret storage. | Secret hygiene scan and backend route tests. | verified |
| SEC-SECRETS-001 | Local production-shaped secrets may exist in ignored env files. | Rotate any real local credentials and keep deploy secrets only in GitHub Environments and Google Secret Manager. | Completed rotation checklist and provider-side evidence for every row. | in_progress |
| MEDIA-GCS-001 | Uploads must use private Cloud Storage, not local public media. | Store profile/chat media as private `gs://` references with signed read URLs. | Backend upload tests, browser upload test using GCS mock, and staging bucket policy/lifecycle proof. | in_progress |
| MEDIA-AUTH-001 | Uploaded media needs authenticated read boundaries and lifecycle policy. | Generate short-lived signed URLs and enforce bucket/prefix scope, quotas, and cleanup. | Unauthorized read denial, signed URL scope tests, and GCS lifecycle evidence. | in_progress |
| RT-FANOUT-001 | Realtime fanout must not silently fail under load. | Use durable outbox processing and Firestore delivery evidence. | Fanout test output and provider delivery artifact. | in_progress |
| RT-OUTBOX-001 | Realtime events need a recoverable worker path. | Keep outbox drain endpoint protected and scheduled worker deployable. | Runtime diagnostics and outbox drain evidence. | verified |
| E2E-STUDENT-001 | Student core journey must be backend-backed. | Keep browser tests against the real FastAPI backend and isolated test DB. | Playwright integration output for login, topic, watch/progress. | verified |
| E2E-PROF-001 | Professor workflows must be backend-backed. | Keep professor action tests against real API routes. | Playwright/backend evidence for professor-to-student visible changes. | verified |
| E2E-UPLOAD-001 | Upload flows must be browser-tested through the backend. | Use GCS mock locally and real GCS evidence in staging. | Playwright upload test and staging upload artifact. | verified |
| E2E-NEGATIVE-001 | Negative states must be covered. | Test expired auth, forbidden access, backend failure, and empty UI. | Playwright negative-state output. | verified |
| PERF-TOPIC-001 | Topic page queries must stay bounded. | Keep query-plan and endpoint tests for topic workspace. | Query audit output against Postgres. | in_progress |
| PERF-WATCH-001 | Watch context must avoid broad enumeration. | Use direct watch-context endpoint and assert route behavior. | Browser/backend integration evidence. | verified |
| PERF-XP-001 | XP writes must remain bounded and idempotent. | Keep XP unit tests and duplicate-event coverage. | Backend test output. | verified |
| PERF-LIVE-001 | Live/chat flows need load evidence. | Run narrow load checks before broad student traffic. | Load test artifact for live/chat paths. | in_progress |
| PERF-PAGE-001 | Lists must paginate. | Keep backend pagination caps for comments/chat/topic lists. | Backend tests for limits and offsets. | verified |
| FE-ERROR-001 | Browser errors must reach backend telemetry. | Keep `/api/client-errors` and redacted metrics. | Frontend/backend tests and deployed smoke evidence. | verified |
| FE-DATA-001 | Frontend data contracts must match backend payloads. | Keep contract tests for core API wrappers and routes. | Test output for API wrapper and route contracts. | verified |
| FE-DEMO-001 | Demo behavior must not leak into production. | Keep production env validation and local-only demo flags. | Production env validator output and browser smoke. | verified |
| OPS-STAGE-001 | Staging must be a first-class release environment. | Keep separate staging GCP project resources, secrets, DB, services, and evidence workflows. | Staging deploy, diagnostics, rollback, and backup/restore artifacts. | in_progress |
| OPS-CLOUDSQL-001 | Managed Postgres connectivity and TLS must be proven. | Require `DATABASE_CONNECTION_STRATEGY=cloud_sql` or `alloydb`, `sslmode=verify-full`, and `PGSSLROOTCERT=certifi`. | Startup validation, diagnostics output, and staging DB connection evidence. | in_progress |
| OPS-CLOUDRUN-001 | Cloud Run runtime settings must match the cost and launch posture. | Keep min instances at 0, bounded max instances, deploy health checks, and stopped-db cleanup. | Deploy workflow logs and Cloud Run service readiness evidence. | verified |
| OPS-RUNBOOK-001 | Operations need rollback and incident procedures. | Keep GCP-only runbook and manual operations docs current. | Runbook drill evidence and incident checklist sign-off. | in_progress |
| OPS-READY-001 | Readiness must include dependency diagnostics. | Keep `/ready` and protected `/api/internal/diagnostics` checks. | Runtime verifier output from staging/dark production. | verified |

## Evidence Log

| Date | Gate | Evidence |
| --- | --- | --- |
| 2026-06-17 | GCP migration cleanup | Branch posture moved to Cloud Run, Cloud SQL or AlloyDB, Firebase Auth, Firestore realtime, Google Secret Manager, and private Cloud Storage media. Remaining incomplete rows require live provider evidence, not old-platform fallback paths. |
| 2026-06-17 | OPS-STAGE-001 / OPS-CLOUDSQL-001 / OPS-CLOUDRUN-001 | Staging Launch Evidence workflow run `27714170323` passed on `codex/merge-all-ahead`: backend and frontend Cloud Run services were Ready with `minScale=0` and `maxScale=3`, Cloud SQL `kresco-staging-postgres` was stopped with `activationPolicy=NEVER`, Artifact Registry cleanup policies were present, and backend health plus frontend HTML smoke checks passed. Rows remain `in_progress` until deployment, rollback, backup/restore, and provider diagnostics evidence are complete. |
| 2026-06-17 | OPS-READY-001 / RT-OUTBOX-001 / SEC-SECRETS-001 | Staging Provider Diagnostics workflow run `27715259924` reached `/ready`, sourced the internal secret from Google Secret Manager runtime config, drained the realtime outbox with zero retry/dead-letter events, and uploaded a `staging-provider-diagnostics` artifact. The run failed correctly because staging lacked VdoCipher live config and CMI payment config; rotation checklist rows stay incomplete. |
| 2026-06-17 | MEDIA-GCS-001 / MEDIA-AUTH-001 | Staging Launch Evidence workflow run `27716619050` captured `MEDIA_GCS_BUCKET` and `MEDIA_GCS_PREFIX` from Google Secret Manager but failed the private media bucket posture collector because `github-deployer@kresco-staging.iam.gserviceaccount.com` lacks `storage.buckets.get` on `kresco-staging-private-media`. The artifact includes `media-bucket-error.json`; media rows stay `in_progress` until deploy IAM can read bucket/IAM posture and lifecycle proof. |
| 2026-06-17 | MEDIA-GCS-001 / MEDIA-AUTH-001 / OPS-STAGE-001 | Staging Launch Evidence workflow run `27717387591` used the aggregated evidence manifest. Cloud Run posture, Cloud SQL cost posture, Artifact Registry cleanup, and runtime smoke passed; only `media_bucket` failed with `required_permission=storage.buckets.get`. This confirms the staging workflow now collects complete evidence even when the media bucket IAM proof is blocked. |
| 2026-06-17 | FE-DEMO-001 | Removed the local demo video branch from the production `VideoPlayer` path, rebuilt the frontend, and scanned `.next/static` plus app server output for banned mock/demo/local strings with zero matches. The frontend deploy workflow now runs `npm run check:production-demo-surface -- --base-url "$FRONTEND_URL" --json` against the deployed Cloud Run URL. The row stays `in_progress` until a staging or dark-production deploy artifact captures that scanner output. |
| 2026-06-17 | PERF-LIVE-001 | Added a staging live/chat load evidence checker and manual workflow for authenticated read-only probes across student live sessions, live interactions, live checkpoints, professor chat status, and chat messages. The row stays `in_progress` until the workflow runs against staging and uploads the `staging-live-chat-load` artifact. |
| 2026-06-17 | OPS-RUNBOOK-001 | Added a staging runbook drill evidence workflow and checker for Cloud Run rollback candidates, protected runtime diagnostics, Cloud SQL backup posture, restore-drill evidence, and incident sign-off. The row stays `in_progress` until the workflow runs with a real managed Postgres restore-drill artifact and uploads `staging-runbook-drill`. |
| 2026-06-18 | SEC-SECRETS-001 / E2E-STUDENT-001 | Code migration moved email/password signup, email verification, password reset, Google login, and SQLAdmin access onto Firebase Auth-backed sessions. Backend custom password routes were removed, app sessions are created only after Firebase ID-token verification at `/api/auth/firebase-session`, and Resend is no longer an auth readiness gate. Rows stay `in_progress` until staging captures provider-side Firebase Auth configuration and deployed login evidence. |
| 2026-06-18 | OPS-STAGE-001 / OPS-CLOUDRUN-001 / MEDIA-GCS-001 / MEDIA-AUTH-001 | Added `Deploy Staging` auto-deploy orchestration for `master`, reusable backend/frontend deploy outputs, lightweight post-deploy smoke checks, and import-first Terraform scaffolding. Workstation verification confirmed deploy service-account impersonation and private media bucket posture read access. Rows stay `in_progress` until the workflow passes on `master` and uploads evidence. |
| 2026-06-18 | SEC-SECRETS-001 / E2E-STUDENT-001 | Identity Toolkit Admin API evidence for `kresco-staging` confirmed Firebase Email/Password, Phone, and Google providers are enabled, with authorized domains covering localhost, Firebase default domains, the staging Cloud Run frontend URL, and `staging.kresco.ma`. `SEC-SECRETS-001` stays `in_progress` until the full rotation checklist, CMI, VdoCipher, rate-limit, and production rows have provider-side evidence. |
| 2026-06-18 | FE-DEMO-001 / OPS-CLOUDRUN-001 / OPS-STAGE-001 | GitHub Actions `Deploy Staging` run `27738912590` passed on `master`: backend CI, frontend CI, backend deploy, frontend deploy, deployed demo-surface scan, and staging smoke all succeeded. Post-run workstation check confirmed Cloud SQL `kresco-staging-postgres` returned to `STOPPED` with `activationPolicy=NEVER`. `OPS-STAGE-001` remains `in_progress` until the separate rollback, backup/restore, and provider diagnostic artifacts are complete. |
