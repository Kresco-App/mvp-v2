# Filière Values Are Controlled Slugs

Filière is stored as a controlled slug rather than free text because it drives filtering, access, content imports, analytics, and exam metadata. Admin UIs can show localized labels, but persisted values use stable slugs such as `pc`, `svt`, `sma`, `smb`, and `eco` to avoid duplicate spellings of the same track.
